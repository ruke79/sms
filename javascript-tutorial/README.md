# javascript-tutorial — 실행되는 자바스크립트 튜토리얼 (중급~고급)

`manuscripts/javascript-면접/` 의 50문항 중 **직접 실행해서 확인할 수 있는 것들**만 골라
레슨으로 만든 모듈입니다. [`java-tutorial/`](../java-tutorial/README.md) 과 같은 발상이고,
**레슨 하나가 테스트 하나**입니다.

```bash
cd javascript-tutorial && npm test          # 16레슨 114건 — 의존 설치 없음
npm run test:one lessons/lesson09_http-client.test.js   # 레슨 하나만
```

현재 **16개 레슨 / 114개 테스트, 전부 통과**합니다 (Node 22.22.2 · 10회 반복 실행 0실패).

레슨은 두 묶음입니다.

| | 다루는 것 | 레슨 | 건수 |
|---|---|---|---|
| **1부. 언어 기제** | 스코프·프로토타입·이벤트 루프·Promise·값·모듈·타이머·스트림 | 1~8 | 58 |
| **2부. 실무 패턴** | 외부 API·동시성 제어·캐시·요청 컨텍스트·에러·시간과 돈·문자열·서버 운영 | 9~16 | 56 |

2부는 **"이 코드를 실제로 쓴다"** 쪽입니다. 진짜 HTTP 서버를 `127.0.0.1` 에 띄워 놓고
타임아웃·재시도·멱등 키·ETag·graceful shutdown 을 돌려 봅니다. 외부 네트워크는 쓰지 않습니다.

---

## 1. 왜 이렇게 만들었나

이 저장소의 원칙은 `docs/00` §8 — **"검증한 척"을 만들지 않는다** 입니다.
`javascript-면접/README.md` 에는 지금까지 "**이 저장소에는 자바스크립트 실행 검증 모듈이 없다**"고
적혀 있었습니다. 그 문장을 지우기 위해 만든 모듈입니다.

- **의존이 0 입니다.** Node 22 내장 러너(`node --test`)와 `node:assert` 만 씁니다.
  `npm install` 이 없으니 공급망도, 잠금 파일도 없습니다.
- **브라우저가 필요한 문항은 여기 없습니다** → 렌더링(Q26)·DOM 이벤트(Q23)·CORS 의 브라우저 쪽(Q30)·
  Web Worker(Q43)는 Node 에서 판정할 수 없습니다. 언어·런타임(Node) 범위만 다룹니다.
- **시간을 재지 않습니다.** 병행/직렬은 "동시에 떠 있던 최대 개수"로, 지연 평가는 "호출 횟수"로,
  백프레셔는 "버퍼에 쌓인 개수"로 잽니다. 전부 장비와 무관하게 결정적입니다.
  디바운스·스로틀은 `node:test` 의 **가짜 타이머**로 시계를 돌려서 단정합니다.

## 2. `fact` / `observe` / `lesson`

[`lessons/lesson.js`](lessons/lesson.js) 에 도구가 세 개 있습니다. `java-tutorial` 의 `Lesson.java` 와 같습니다.

| 도구 | 뜻 | 판정에 쓰이나 |
|---|---|---|
| `fact(라벨, 값)` | 확인된 값을 콘솔에 남긴다 | 아니오 (기록용) |
| `observe(라벨, 값)` | **환경에 좌우되는 값**. 단정하지 않는다 | 아니오 |
| `lesson(문장)` | 레슨의 결론 한 줄 | 아니오 |

판정은 오직 `assert` 가 합니다. **테스트가 초록이라고 해서 `observe` 로 찍힌 줄까지 증명된 것은 아닙니다.**
현재 `observe` 는 **5곳**입니다 — 3-6(메인에서의 `setTimeout` vs `setImmediate` 순서), 3-7(I/O 완료 순서),
7-4(GC 뒤 `WeakRef.deref()`), 7-5(`FinalizationRegistry` 콜백 도착), 8-6(워커가 도는 동안 메인 타이머 횟수).

예를 들어 7-4 는 이렇게 나옵니다.

```
   ~ 강한 참조를 끊고 gc() 뒤 WeakRef.deref()           = undefined (회수됨)   (환경 의존 — 단정하지 않음)
```

단정한 것은 **`Map` 이 잡고 있는 한 절대 회수되지 않는다**뿐입니다. "끊으면 회수된다"는
`--expose-gc` 로 강제해도 사양이 약속하지 않으므로 단정하지 않았습니다.

## 3. 레슨 목록

숫자는 `manuscripts/javascript-면접/` 의 문항 번호입니다. ★ 는 변별력이 크거나 실무 사고로 이어지는 항목입니다.

### 레슨 1. 스코프·클로저·this (8개)
`lesson01_scope-closure-this.test.js` — Q1 · Q3 · Q4

| | 레슨 | |
|---|---|---|
| 1-1 | `var` 는 끌어올려져 `undefined`, `let` 은 TDZ 로 `ReferenceError` | Q1 |
| 1-2 | `var` 는 함수 스코프, `let` 은 블록 스코프 | Q1 |
| 1-3 | **`const` 는 재대입만 막는다** — `Object.freeze` 도 얕다 | Q1 ★ |
| 1-4 | **루프의 `var` 클로저는 전부 같은 변수를 본다** (`[3,3,3]` vs `[0,1,2]`) | Q1·Q3 ★ |
| 1-5 | 클로저로 밖에서 닿을 수 없는 상태를 만든다 | Q3 |
| 1-6 | **`this` 는 호출 방식이 정한다 — 그리고 `bind` 는 한 번만 먹는다** | Q4 ★ |
| 1-7 | **메서드를 떼어내면 `this` 가 사라진다** — 화살표 필드는 붙어 오되 인스턴스마다 함수가 생긴다 | Q4 ★ |
| 1-8 | 화살표 함수는 `call` 로도 `this` 를 못 바꾼다 | Q4 |

### 레슨 2. 프로토타입 — `class` 의 속과 오염 (7개)
`lesson02_prototype.test.js` — Q5 · Q11 · Q15 · Q38

| | 레슨 | |
|---|---|---|
| 2-1 | `class` 는 프로토타입의 문법 설탕 — 체인 끝은 `null` | Q5 |
| 2-2 | 인스턴스 속성이 프로토타입 속성을 가린다(shadowing) | Q5 |
| 2-3 | `hasOwn` · `in` · `for-in` 은 다른 것을 본다 | Q5 |
| 2-4 | **`{}` 를 사전으로 쓰면 `"constructor"` 키에서 사고가 난다** — `Map` 은 정직하다 | Q15 ★ |
| 2-5a | **순진한 재귀 병합이 `__proto__` 를 타고 전역을 오염시킨다** — 실제로 `{}.isAdmin === true` 를 만든다 | Q38 ★ |
| 2-5b | `Object.create(null)` 과 `Map` 은 같은 입력에도 오염되지 않는다 | Q38 |
| 2-6 | `Object.freeze` 는 얕다 — 깊게 얼리려면 재귀 | Q11 |

> 2-5a 는 `Object.prototype` 을 **실제로 오염**시킵니다. `afterEach` 에서 반드시 되돌립니다.

### 레슨 3. 이벤트 루프 — 무엇이 먼저 도는가 (7개)
`lesson03_event-loop.test.js` — Q6 · Q32 · Q33

| | 레슨 | |
|---|---|---|
| 3-1 | **동기 → 마이크로태스크 전부 → 매크로태스크 하나** | Q6 ★ |
| 3-2 | **마이크로태스크는 "비워질 때까지"** — 1,000번 연쇄 `then` 이 타이머를 굶긴다 | Q6 ★ |
| 3-3 | `async` 의 동기 부분은 즉시, `await` 마다 마이크로태스크 하나 | Q6·Q9 |
| 3-4 | **`process.nextTick` 은 "매크로태스크에서 출발했을 때만" 마이크로태스크보다 먼저** | Q32 ★ |
| 3-5 | **동기 코드가 도는 동안 타이머는 절대 못 낀다** | Q6·Q33 ★ |
| 3-6 | `setTimeout(0)` vs `setImmediate` — 메인에서는 비결정(`observe`), I/O 콜백 안에서는 `immediate` 가 먼저 | Q32 |
| 3-7 | I/O 완료 순서는 단정하지 않는다 — "동기가 맨 앞"만 단정 | Q6 |

> **3-4 는 이 튜토리얼을 만들다 실제로 틀린 것입니다.** 처음에 테스트 본문에 세 줄을 바로 쓰고
> `nextTick` 이 먼저일 거라 단정했더니 **맨 뒤**에 왔습니다. 테스트 본문이 이미 마이크로태스크(Promise 연속)
> 안에서 돌고 있어서, V8 이 마이크로태스크 큐를 다 비운 뒤에야 Node 가 `nextTick` 큐를 봅니다.
> "nextTick 이 항상 먼저"는 틀린 문장이라, **출발점을 나눠 두 경우를 모두 단정**하는 레슨으로 바꿨습니다.

### 레슨 4. Promise — 기다린 척과 진짜 기다림 (9개)
`lesson04_promise.test.js` — Q7 · Q8 · Q9 · Q10

| | 레슨 | |
|---|---|---|
| 4-1 | 상태 전이는 한 번뿐 — 두 번째 `resolve`/`reject` 는 무시 | Q7 |
| 4-2 | **`then` 안에서 `return` 을 빼먹으면 기다리지 않고 지나간다** | Q7 ★ |
| 4-3 | `then` 이 Promise 를 반환하면 평탄화된다 | Q7 |
| 4-4 | `catch` 로 복구하면 체인은 fulfilled 로 계속된다 | Q7·Q9 |
| 4-5 | **`await` 를 빠뜨린 Promise 는 `try/catch` 로 안 잡히고 프로세스가 죽는다** — 자식 프로세스의 종료 코드 1 로 확인 | Q9 ★ |
| 4-6 | **`Promise.all` 은 하나만 실패해도 즉시 reject — 그러나 나머지는 취소되지 않는다** | Q8 ★ |
| 4-7 | **루프 안 `await` 는 직렬(최대 동시 1), 먼저 만들어 `all` 에 넘기면 병행(최대 동시 5)** | Q10 ★ |
| 4-8 | **동시 실행 수를 2로 제한하는 풀** — 최대 동시 2 를 단정 | Q10 ★ |
| 4-9 | executor 의 동기 `throw` 는 reject 가 되지만 콜백 안의 `throw` 는 못 잡는다 | Q9 |

> **4-5 도 처음 방식이 틀렸습니다.** 같은 프로세스 안에서 `process.on("unhandledRejection")` 으로 잡으려
> 했는데, `node:test` 러너가 같은 이벤트를 듣고 테스트를 실패로 판정합니다. 자식 프로세스로 띄워
> **종료 코드**로 보는 쪽으로 바꿨고, 그 덕에 "Node 15+ 는 기본이 크래시"가 그대로 보입니다.

### 레슨 5. 값 — 복사·비교·숫자 (8개)
`lesson05_values.test.js` — Q2 · Q11 · Q12 · Q14 · Q20

| | 레슨 | |
|---|---|---|
| 5-1 | `==` 는 형 변환 뒤 비교 — `NaN` 은 `Object.is` | Q2 |
| 5-2 | **스프레드는 얕은 복사 — 안쪽은 공유된다** | Q11 ★ |
| 5-3 | **JSON 왕복은 `Date`·`undefined`·`Map`·순환에서 깨지고, `structuredClone` 은 함수에서 막힌다** | Q11 ★ |
| 5-4 | **`sort()` 의 기본 비교는 문자열** (`[1,10,100,9]`) — 그리고 원본을 바꾼다. `toSorted` 는 아니다 | Q14 ★ |
| 5-5 | 원본을 바꾸는 메서드 / 새 배열을 주는 메서드 | Q14 |
| 5-6 | **`??` 는 `0`·`""` 을 살리고 `\|\|` 는 지운다** | Q12 ★ |
| 5-7 | **`JSON.parse` 가 64비트 ID 끝자리를 조용히 바꾼다** — `BigInt` 는 정확하되 `Number` 와 안 섞인다 | Q20 ★ |
| 5-8 | `typeof null === 'object'` — 배열은 `Array.isArray` | Q2 |

### 레슨 6. 이터레이터·제너레이터·모듈 (6개)
`lesson06_iterators-modules.test.js` — Q16 · Q17 · Q34

| | 레슨 | |
|---|---|---|
| 6-1 | **제너레이터는 요청한 만큼만 계산한다** — 무한 수열에서 5개, 계산 횟수 5 | Q17 ★ |
| 6-2 | `Symbol.iterator` 하나로 `for-of`·스프레드·구조 분해가 전부 된다 | Q17 |
| 6-3 | **`for-of` 의 `break` 는 제너레이터의 `finally` 를 실행한다** — `next()` 만 부르고 버리면 안 된다 | Q17 ★ |
| 6-4 | 비동기 제너레이터 + `for await` — 소비자가 3개 받으면 생산자도 3개만 만든다 | Q17·Q34 |
| 6-5 | **ESM `import` 는 라이브 바인딩, CJS `require` 는 값 복사** — 픽스처 모듈 두 개로 확인 | Q16 ★ |
| 6-6 | 모듈은 한 번만 평가된다 · 네임스페이스는 봉인 · 톱레벨 `await` | Q16 |

### 레슨 7. 타이머·메모리·취소 (6개)
`lesson07_timers-memory-cancel.test.js` — Q8 · Q10 · Q18 · Q19

| | 레슨 | |
|---|---|---|
| 7-1 | **디바운스 — 150ms 동안 3번 입력해도 마지막 것 한 번** (가짜 타이머) | Q18 ★ |
| 7-2 | **스로틀 — 110ms 동안 5번 입력하면 `t0`·`t100` 두 번** (가짜 `Date`) | Q18 ★ |
| 7-3 | **해제 안 한 리스너는 렌더마다 쌓인다** — `off` 짝 맞추기, `AbortSignal` 로 한꺼번에 | Q19 ★ |
| 7-4 | `WeakMap`/`WeakRef` 는 키를 안 잡는다 — 단, "회수됐다"는 `observe` | Q19 |
| 7-5 | `FinalizationRegistry` 콜백은 언제 올지 모른다 — 정리 로직을 걸지 않는다 (`observe`) | Q19 |
| 7-6 | **`AbortController` 로 4-6 의 살아남은 작업을 실제로 멈춘다** — `AbortSignal.any`·`timeout` | Q8·Q10 ★ |

### 레슨 8. 스트림·백프레셔·워커 (7개) — Node 고급
`lesson08_streams-workers.test.js` — Q17 · Q33 · Q34 · Q36

| | 레슨 | |
|---|---|---|
| 8-1 | **`write()` 의 `false` 가 백프레셔 신호** — `highWaterMark=3` 에서 세 번째가 `false`, `drain` 뒤 0 | Q34 ★ |
| 8-2 | **반환값을 무시한 루프는 200개가 통째로 버퍼에, `pipeline` 은 상한 5 안에서 흐른다** | Q34 ★ |
| 8-3 | `pipeline` 은 에러를 전파하고 소스를 `destroy` 한다 — **닫히면서 버퍼에 있던 것은 버려진다** | Q34·Q36 |
| 8-4 | **청크 경계는 줄 경계가 아니다** — `'ab\ncd'`+`'ef\ngh'` 에서 `cdef` 를 복원하는 `Transform` | Q34 ★ |
| 8-5 | `Readable.from(async 제너레이터)` 와 `for await` — 스트림은 AsyncIterable | Q17·Q34 |
| 8-6 | **메인에서 `fib(30)` 이 도는 동안 타이머 0회, 워커에서 돌면 0회가 아니다** (횟수는 `observe`) | Q33 ★ |
| 8-7 | 워커를 못 쓰면 청크 사이에 `await setImmediate()` — 타이머가 중간에 낀다 | Q33 |

> **8-2·8-3·8-7 은 셋 다 첫 판에서 제 단정이 틀렸습니다.**
> - 8-2: `_write` 안에서 잰 최대치가 200 이 아니라 **199** 였습니다 — 두 번째 `_write` 시점엔 첫 청크가 이미
>   빠진 뒤라서요. "몇 개 쌓였나"는 어디서 재느냐에 따라 1 이 다릅니다. 루프 직후 값(200)으로 단정하도록 고쳤습니다.
> - 8-3: 도착한 것이 `['A','B']` 가 아니라 **`['A']`** 였습니다. B 는 버퍼에 들어갔지만 `pipeline` 이 sink 를
>   `destroy` 하면서 버퍼째 버렸습니다. **에러 시 버퍼는 사라진다** — 그 자체가 레슨이라 그렇게 남겼습니다.
> - 8-7: 단독으로는 통과하고 **8-6 뒤에 이어 돌 때만** 실패했습니다. 8-6 이 워커의 `message`(poll 단계) 콜백에서
>   끝나서 8-7 이 poll 단계에서 출발하고, 그러면 `setImmediate` 가 같은 바퀴의 check 단계에서 돌아 타이머보다
>   먼저 옵니다. **3-6 에서 단정한 규칙에 제가 걸린 것**이라, 타이머로 기다리게 고치고 주석으로 남겼습니다.

---

# 2부. 실무 패턴 (레슨 9~16)

여기부터는 **문법이 아니라 설계**입니다. 면접의 뒤쪽 문항(Q29·Q35·Q44~Q48)이 이 구간이고,
그쪽은 "안다"보다 **"틀리는 쪽을 아는가"** 를 봅니다. 그래서 레슨마다 **잘못된 판을 먼저 실행**합니다.

로컬 HTTP 서버는 [`lessons/testserver.js`](lessons/testserver.js) 가 `127.0.0.1` 의 임의 포트에 띄웁니다.
**외부 네트워크를 쓰지 않습니다** — CI 가 남의 사정으로 빨개지면 안 되니까요.
판정은 전부 **호출 횟수·동시 개수·상태 코드**이고 시간은 재지 않습니다.

### 레슨 9. 외부 API — 타임아웃·재시도·멱등성 (7개)
`lesson09_http-client.test.js` — Q35

| | 레슨 | |
|---|---|---|
| 9-1 | **`fetch` 에는 기본 타임아웃이 없다** — 안 걸면 안 돌아온다. `AbortSignal.timeout` 은 `TimeoutError` | Q35 ★ |
| 9-2 | **지터 없는 백오프는 100대가 같은 순간에 다시 온다** (서로 다른 대기 시간 1개 vs 50개 초과) | Q35 ★ |
| 9-3 | 5xx 는 재시도(3회 만에 성공), **4xx 는 한 번만** — 400 을 재시도하면 상대만 괴롭힌다 | Q35 ★ |
| 9-4 | `Retry-After: 7` 을 주면 자기 백오프(150·300ms)가 아니라 **7초를 따른다** | Q35 |
| 9-5 | **POST 를 재시도하면 주문이 두 건 생긴다** — 실패한 것은 응답이지 처리가 아니다 | Q35 ★ |
| 9-6 | `Idempotency-Key` 를 주면 재시도해도 **한 건**, 그리고 처음과 같은 응답이 재생된다 | Q35 ★ |
| 9-7 | 타임아웃 1초 + 3회 재시도 = **최악 4.7초** — 전체 마감을 따로 두면 시도가 2회로 준다 | Q35 |

### 레슨 10. 동시 실행 제어 (7개)
`lesson10_concurrency.test.js` — Q33 · Q35 · Q36

| | 레슨 | |
|---|---|---|
| 10-1 | **`Promise.all` 은 병렬이 아니라 "전부 동시에"** — 작업 50개면 동시 50개 | Q33·Q35 ★ |
| 10-2 | 상한을 두면 **최대 동시만 5로 줄고 처리 건수는 그대로 50** | Q35 ★ |
| 10-3 | 같은 키 동시 요청 10건 → **원본 호출 10회가 1회로** (진행 중인 Promise 를 공유) | Q29·Q35 ★ |
| 10-4 | `Promise.all` 은 첫 실패에서 나머지 성공을 버린다 — `allSettled` 는 수확한다 | Q36 ★ |
| 10-5 | **취소는 아직 시작 안 한 것만 막는다** — 이미 출발한 2개는 끝난다 | Q35 |
| 10-6 | 큐에 상한이 없으면 대기열만 자란다 — 상한 10, 거부 20 | Q33 |
| 10-7 | 순차 처리는 실패 하나가 뒤를 전부 막는다 (`[1,2]` 에서 멈춤) | Q35 |

### 레슨 11. 캐시 — 상한·TTL·스탬피드·ETag (7개)
`lesson11_cache-http.test.js` — Q29 · Q48

| | 레슨 | |
|---|---|---|
| 11-1 | **상한 없는 캐시는 기능이 아니라 누수** — 10,000개 vs 100개 | Q29 ★ |
| 11-2 | LRU 는 읽기만 해도 순서가 바뀐다 (`Map` 이 삽입 순서를 보장하므로 가능) | Q29 |
| 11-3 | TTL 은 **가짜 시계**로 검증한다 — 999ms 살아 있고 1000ms 에 사라진다 | Q29 |
| 11-4 | **캐시 스탬피드** — 동시 미스 20건이 원본을 20번 부른다. 약속을 공유하면 1번 | Q29 ★ |
| 11-5 | **실패까지 캐시하면 복구된 뒤에도 장애가 이어진다** — 성공만 캐시한다 | Q29·Q36 |
| 11-6 | ETag/304 — 두 번째 요청의 본문 길이가 **0**, 서버가 본문을 보낸 건 1회 | Q29 ★ |
| 11-7 | `Cache-Control` 이 없으면 중간 계층이 추측한다 — 개인 데이터는 `private, no-store` | Q29 |

### 레슨 12. 요청 컨텍스트 — 전역 변수는 왜 섞이나 (7개)
`lesson12_request-context.test.js` — Q46 · Q47

| | 레슨 | |
|---|---|---|
| 12-1 | **전역 변수에 담으면 `['C','C','C']` 로 덮인다** — 싱글 스레드라도 `await` 사이에 끼어든다 | Q46 ★ |
| 12-2 | `AsyncLocalStorage` 는 요청마다 따로 흐른다 (`['A','B','C']`) | Q46 ★ |
| 12-3 | `await`·`setTimeout`·`nextTick`·마이크로태스크를 건너도 유지된다 | Q46 |
| 12-4 | 컨텍스트 밖에서는 **에러가 아니라 조용히 없다** | Q46 |
| 12-5 | **워커 스레드로는 안 넘어간다** — 손으로 넘겨야 보인다 | Q46 ★ |
| 12-6 | 구조화 로그 vs 평문 — `user=42` 정규식이 `user=421` 을 잡는 경계 문제 | Q46 ★ |
| 12-7 | 개인정보는 남기기 전에 지운다 — 식별자는 남기고 이메일·카드는 마스킹 | Q46 |

> **12-1 과 12-5 는 Spring 의 Q37·Q48·Q80 과 같은 함정입니다.** 저쪽은 `ThreadLocal` 이 스레드를 못 넘고
> 이쪽은 `AsyncLocalStorage` 가 워커를 못 넘습니다. 언어가 바뀌어도 경계의 모양은 같습니다.

### 레슨 13. 에러 — 원인 사슬과 경계 (7개)
`lesson13_errors.test.js` — Q36 · Q47

| | 레슨 | |
|---|---|---|
| 13-1 | `cause` 를 안 넘기면 원인이 사라진다 — 넘기면 사슬을 끝까지 따라간다 | Q36 ★ |
| 13-2 | **Error 가 아닌 것도 던져진다** — 문자열을 던지면 `e.message` 가 `undefined` | Q36 ★ |
| 13-3 | **`finally` 의 `return` 이 예외를 지운다** — Java Q20 이 그대로 재현된다 | Q36 ★ |
| 13-4 | `Promise.any` 는 `AggregateError` 로 실패 전부를 들고 온다 | Q36 |
| 13-5 | `await` 하지 않은 async 에러는 **호출자가 반환한 뒤에** 도착한다 | Q36 ★ |
| 13-6 | 잡히지 않은 rejection 은 프로세스를 죽인다 (종료 코드 1 vs 0) | Q36·Q47 ★ |
| 13-7 | 재시도 가능/불가를 **타입으로** 가른다 (429·5xx만 재시도) | Q35·Q36 |

### 레슨 14. 시간·타임존·돈 (7개)
`lesson14_time-money.test.js` — Q44 · Q45

| | 레슨 | |
|---|---|---|
| 14-1 | 같은 한 점이 **도쿄에서 15일, 뉴욕에서 14일** — "1월 15일 매출"은 존을 정해야 답이 있다 | Q44 ★ |
| 14-2 | `2026-01-15` 는 UTC 자정, `2026-01-15T00:00:00` 은 로컬 — 파싱 규칙이 다르다 | Q44 ★ |
| 14-3 | 1월 31일 + 1개월 = **3월 3일** — 월말은 잘라내야 한다 | Q44 ★ |
| 14-4 | JSON 왕복에서 `Date` 가 문자열이 된다 (`.getTime is not a function` 의 자리) | Q44 |
| 14-5 | **어긋나는 조합과 우연히 맞는 조합이 둘 다 있다** — 그래서 운 좋은 데이터로 짠 테스트는 통과한다 | Q45 ★ |
| 14-6 | `(1.005).toFixed(2) === '1.00'` — 계산 반올림과 표시 반올림을 나눈다 | Q45 |
| 14-7 | 기본 `sort` 는 대문자를 전부 앞으로 보낸다 — `localeCompare` | Q45 |

### 레슨 15. 문자열·입력 검증 (7개)
`lesson15_strings-input.test.js` — Q45 · Q38

| | 레슨 | |
|---|---|---|
| 15-1 | **`'👨‍👩‍👧‍👦'.length === 11`** — 코드 유닛 / 코드 포인트 / grapheme 이 전부 다르다 | Q45 ★ |
| 15-2 | `slice` 는 서로게이트를 자른다 — 미리보기 끝의 깨진 글자가 이것 | Q45 ★ |
| 15-3 | **눈에 같은데 `!==`** — NFC/NFD 를 정규화하지 않으면 검색이 안 된다 | Q45 ★ |
| 15-4 | 글자 9자가 **23바이트** — 앱은 글자로, DB 는 바이트로 제한하면 프로덕션에서 터진다 | Q45 |
| 15-5 | `path.join(root, '../../etc/passwd')` → `/etc/passwd` — 최종 경로를 확인해야 막힌다 | — |
| 15-6 | 비밀 값은 `===` 가 아니라 `timingSafeEqual`, 비밀번호는 `scrypt` | — |
| 15-7 | **프로토타입 오염은 파싱이 아니라 병합에서** — 위험 키를 경계에서 떨어뜨린다 | Q38 ★ |

### 레슨 16. 서버 운영 — 내리는 순서가 곧 가용성 (7개)
`lesson16_server-ops.test.js` — Q46 · Q47

| | 레슨 | |
|---|---|---|
| 16-1 | 그냥 죽이면 처리 중 5건이 **전부 실패** — 배포마다 5xx 가 나는 이유 | Q46 ★ |
| 16-2 | graceful 은 5건을 전부 200 으로 끝내고 새 연결만 막는다 | Q46 ★ |
| 16-3 | **liveness 200 / readiness 503** — 살아 있지만 트래픽은 안 받는 상태 | Q46 ★ |
| 16-4 | **liveness 에 DB 를 넣으면 전 Pod 이 동시에 재시작한다** — 의존은 readiness 에만 | Q46 ★ |
| 16-5 | 종료에도 상한이 필요하다 — 5초짜리 요청을 끝까지 기다리지 않는다 | Q46 |
| 16-6 | 진행 중 요청 수를 모르면 "몇 초 기다린다"는 추측이 된다 | Q46·Q47 |
| 16-7 | 상류 타임아웃이 없으면 게이트웨이가 붙잡힌다 — 504 로 빨리 실패시킨다 | Q35·Q46 |

> **16-3·16-4 는 Kubernetes Q11·Q13 과 Spring Q117 이 코드가 된 것입니다.**
> "프로세스가 살아 있는가"와 "요청을 받아도 되는가"가 다른 질문이라는 것을 상태 코드로 확인합니다.

> **2부에서도 네 번 틀렸습니다.**
> - **9-4**: `server.calls.length` 로 "몇 번째 호출인가"를 셌더니 첫 요청에서 이미 1 이었습니다.
>   기록이 핸들러보다 **먼저** 쌓이기 때문입니다. 명시적 카운터로 바꿨습니다.
> - **10-5**: 취소하면 0건이 실행될 거라 단정했는데 **2건**이 실행됐습니다. async 함수 본문이
>   **첫 `await` 까지 동기로** 달려서, `abort()` 전에 이미 상한만큼이 검사를 지나가 있었습니다.
>   "취소했다"와 "부수효과가 없다"가 다르다는 뜻이라 그대로 레슨이 됐습니다.
> - **14-5**: 부동소수점 합계가 어긋난다고 단정했는데 고른 숫자가 **우연히 딱 떨어졌습니다.**
>   어긋나는 조합과 안 어긋나는 조합을 둘 다 남겼습니다 — 그게 이 버그의 진짜 성질입니다.
> - **15-4**: 바이트 길이를 21 이라 썼는데 **23** 이었습니다. 공백 2개를 빼먹었습니다.
>   "한글은 3바이트"만 외우고 있으면 이렇게 어긋납니다.

## 4. 학습 순서

**1부**는 1 → 2 가 "언어의 바닥"이고, **3 → 4 → 7 이 한 덩어리**(이벤트 루프 → Promise → 취소)입니다.
5 는 독립적이라 아무 때나 봐도 되고, 6 → 8 은 "이터레이터 → 스트림"으로 이어집니다.

**2부**는 **9 → 10 → 11 이 한 덩어리**입니다(외부 호출 → 동시성 → 캐시). 셋 다 "상대를 지키는 법"이고,
9-5·10-3·11-4 가 같은 문제를 세 각도에서 봅니다. 12 → 13 → 16 은 운영 쪽(컨텍스트 → 에러 → 종료),
14 → 15 는 독립적이라 아무 때나 봐도 됩니다.

면접 직전이라면 **3·4**(언어)와 **9·16**(실무)만 보면 됩니다.

```bash
npm run test:one lessons/lesson09_http-client.test.js
node --test --test-name-pattern="9-5" lessons/lesson09_http-client.test.js   # 한 건만
```

## 5. 정직한 고지

- **50문항 중 31문항이 레슨에 걸렸습니다.** 1부가 24문항(Q1~12·14~20·32~34·36·38), 2부가 7문항을
  더했습니다(Q29·Q35·Q44·Q45·Q46·Q47·Q48). 남은 19문항은 브라우저가 필요하거나(Q23·Q25~28·Q30·Q31·Q40~43),
  TypeScript(Q21·22)이거나, 실행으로 판정할 수 없는 경험 문항(Q37·Q39·Q49·Q50)입니다.
  커버리지가 아니라 **판정 가능성**으로 골랐습니다.
- **레슨 15-5·15-6 은 50문항 밖입니다.** 경로 탈출과 타이밍 안전 비교는 문항에 없지만 실무에서 자주
  물어보는 것이라 넣었습니다. 문항 번호를 억지로 붙이지 않고 `—` 로 두었습니다.
- **2부의 HTTP 는 전부 `127.0.0.1` 입니다.** 외부 API 를 부르지 않으므로, 여기서 확인한 것은
  **클라이언트 쪽 규칙**(타임아웃·재시도·멱등 키)이지 실제 상용 API 의 동작이 아닙니다.
- **16-5 만 시간에 기댑니다.** "5초짜리 요청을 끝까지 기다리지 않았다"를 3초 미만으로 단정합니다.
  여유가 크지만, 엄밀히는 장비가 극단적으로 느리면 흔들릴 수 있는 유일한 자리입니다.
- **`observe` 로 찍힌 5줄은 검증되지 않았습니다.** 특히 7-4·7-5 의 GC 관측은 `--expose-gc` 로 강제해도
  "회수된다"를 보장하지 않습니다. 단정한 것은 그 반대(강한 참조가 있으면 회수되지 않는다)뿐입니다.
- **8-6 의 "워커가 도는 동안 타이머가 0회가 아니다"는 타이밍에 기댑니다.** 워커 기동만으로도 수십 ms 가
  걸려 사실상 항상 참이지만, 엄밀히는 `fib(30)` 이 타이머 한 틱보다 오래 걸린다는 가정 위에 있습니다.
  횟수 자체는 `observe` 했습니다.
- **Node 22.22.2 에서만 돌렸습니다.** `t.mock.timers`(7-1·7-2)·`toSorted`(5-4)·`AbortSignal.any`(7-6)·
  `structuredClone`(5-3) 은 Node 20 미만에서 없거나 다릅니다. `package.json` 에 `engines: node >= 22` 로 적어 뒀습니다.
- **성능 수치는 하나도 없습니다.** "빠르다/느리다"는 이 모듈이 다루지 않습니다 — `docs/02` §1-2 의 규칙대로
  시간 측정이 필요한 주장은 여기 넣지 않았습니다.

## 6. 관련 문서

- [`../manuscripts/javascript-면접/README.md`](../manuscripts/javascript-면접/README.md) — 짝이 되는 50문항
- [`../manuscripts/javascript-면접/필수-키노트.md`](../manuscripts/javascript-면접/필수-키노트.md) — S/A/B 등급과 일본어 한 문장
- [`../java-tutorial/README.md`](../java-tutorial/README.md) — 같은 형식의 자바 튜토리얼
- [`../docs/11-CI-CD-와-EC2-프리티어.md`](../docs/11-CI-CD-와-EC2-프리티어.md) — CI 의 `js-tutorial` 잡
