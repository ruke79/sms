# JavaScript 면접 답변 스크립트 — Q1~Q50

[`java-면접/`](../java-면접/README.md)·[`spring-면접/`](../spring-면접/README.md) 과 같은 형식의 별도 세트다.
**일본어 답변 + 한국어 답변** 양쪽을 담았고, 한 문항은 **30~40초 분량**이다.

- **50문항 이내로 압축했다.** 프레임워크(React·Vue) 고유 문항은 넣지 않았다 — 회사마다 다르고,
  언어와 런타임의 이해가 먼저 물어지기 때문이다.
- TypeScript 는 언어 파트에 두 문항(Q21·Q22)만 넣었다.
- 문체는 다른 세트와 같은 **です・ます체**.

> ## ⏱ 시간이 부족하면 → [`필수-키노트.md`](필수-키노트.md)
>
> 50문항을 **S급 10 / A급 15 / B급 8** 세 등급으로 나누고,
> S급에는 **그대로 외울 수 있는 일본어 한 문장**을 붙였습니다.
> **20분밖에 없으면 S급 10문항의 일본어 문장만** 보면 됩니다.
> 끝에 「면접 30분 전 최종 점검」으로 **함정 질문의 "아니오" 목록**을 정리해 뒀습니다.
>
> 그 S급만 뽑은 **[플래시카드](../플래시카드/README.md)** 도 있습니다(`javascript.tsv`, 10장 · Anki 로 바로 가져올 수 있음).

## 구성

| 파일 | 범위 | 문항 | 주제 |
|---|---|---|---|
| [`Part1.md`](Part1.md) | Q1~Q25 | 25 | 언어 핵심 — 스코프, `this`, 프로토타입, 이벤트 루프, Promise, 불변성, 모듈, 타입스크립트, XSS |
| [`Part2.md`](Part2.md) | Q26~Q50 | 25 | 브라우저·Node·실무 — 렌더링, Web Vitals, 번들·캐시, CORS/CSRF, Node 이벤트 루프, 스트림, 인증, 모니터링 |
| | | **50** | |

## 시간이 없다면 이 여덟 개

| | 문항 |
|---|---|
| 거의 반드시 | Q4 `this` 바인딩 · Q6 이벤트 루프와 마이크로태스크 · Q3 클로저 · Q7 Promise 체인 |
| 준비된 사람이 적음 | Q24 토큰을 `localStorage` 에 두면 안 되는 이유 · Q33 Node 에서 CPU 작업 · Q34 백프레셔 · Q38 프로토타입 오염 |

**Q6 은 세트로 준비하는 편이 좋다.** "`setTimeout(fn, 0)` 과 `Promise.resolve().then(fn)` 중 어느 쪽이 먼저인가"
로 이어지는 경우가 많고, 마이크로태스크 큐를 설명할 수 있으면 그 자리에서 끝난다.

## 이 세트의 성격 — 답변의 근거

이 50문항은 **사양·MDN·실무 경험에 근거한 서술**이고 `PERF-*` 같은 케이스 id 를 붙일 수 있는 문항은 없다.
다만 2026-09-06 부터 **[`javascript-tutorial/`](../../javascript-tutorial/README.md)** 이 있다 — 50문항 중
**31문항의 주장을 Node 22 에서 실행으로 확인한 16레슨 114건**이다.

- **1부(레슨 1~8, 58건)** — 언어 기제 24문항(Q1~12·14~20·32~34·36·38). 이벤트 루프 순서(Q6)·`await` 누락(Q9)·
  프로토타입 오염(Q38)·백프레셔(Q34)처럼 "말로 설명하기 쉬운데 실제로 돌려 본 사람은 적은" 문항이 여기 있다.
- **2부(레슨 9~16, 56건)** — 실무 설계 7문항(Q29·Q35·Q44~Q48). 로컬 HTTP 서버를 띄워 놓고 **타임아웃 없는 fetch 가
  안 돌아오는 것**, **POST 재시도로 주문이 두 건 생기는 것**, 멱등 키가 그걸 막는 것, ETag 로 본문이 0바이트가 되는 것,
  전역 변수에 담은 요청 정보가 섞이는 것, graceful shutdown 이 5xx 를 없애는 것을 전부 실행해서 본다.

면접 전에 한 번 돌려 보고 **"직접 재현해 봤는데"** 로 말할 수 있게 해 두는 편이 낫다.
브라우저가 필요한 문항(렌더링·DOM·CORS 의 브라우저 쪽)과 경험 문항(Q37·Q39·Q49·Q50)은 그 모듈에도 없다.

> **수치는 여전히 말하지 않는다.** 튜토리얼도 시간을 재지 않고 순서·횟수·개수로만 단정한다.
> "몇 배 빠르다" 대신 "왜 그런 성질인가"로 답하게 썼다.
> 면접에서 숫자를 인용해야 한다면 그 자리에서 잰 것인지 읽은 것인지 밝히는 편이 안전하다
> (`docs/00` §8 의 정직성 원칙).

다만 **브라우저 보안 문항(Q25 XSS · Q31 CSRF · Q30 CORS)은 서버 쪽 대응 케이스가 이 저장소에 있다** —
`SEC-01`(CSRF)·`SEC-02`(XSS)·`SEC-03`(CORS 프리플라이트). 같은 주제를 서버에서 실행으로 확인한 것이라,
면접에서 "프런트에서는 이렇게, 서버에서는 이렇게 막습니다"로 이어 말할 수 있다.

## 일본어 품질에 대한 고지

`docs/12-일본어-동사-전수-조사.md` 의 방침에 따라 **です・ます체로 통일**했지만 **원어민 검수를 받지 않았다.**
특히 は/が 의 선택은 기계로 판정할 수 없어 검사하지 못했다.

## 관련 문서

- [`../python-면접/README.md`](../python-면접/README.md) · [`../db-면접/README.md`](../db-면접/README.md) · [`../kafka-면접/README.md`](../kafka-면접/README.md) · [`../kubernetes-면접/README.md`](../kubernetes-면접/README.md) — 같은 형식의 다른 세트
- [`../면접-용어-정의.md`](../면접-용어-정의.md) — Java·Spring 용어 사전
- [`../README.md`](../README.md) — 원고 전체 안내
