# 시니어 면접 답변 스크립트 — 회화체 (Part 8 / Q116~Q135)
## 행동 면접 · FinOps · 시스템 설계 심화 · 과제 리뷰 방어 · 시니어리티와 조직 문화

> **이 파일은 `manuscripts/수정본/Part8.md` 를 회화체로 다시 쓴 것입니다.**
> 말투를 바꾸고, **면접에서 1분 안에 말할 수 있는 분량으로 줄였습니다.** 방침은 `회화체/Part1.md` 머리말과 같습니다.
>
> ⚠️ **재출제 3건**: Q119(=Q85 블레임리스), Q123(=Q41·43 Kafka 순서), Q134(=Q9 Disagree and Commit)는 다른 각도로 보강했습니다.
>
> ⚠️ **분량을 줄인 판이라, 근거의 전문은 [`../수정본/Part8.md`](../수정본/Part8.md) 에 있습니다.**

---

# Part 18. 행동 면접

### Q116. 最も大きな失敗経験と教訓
(가장 큰 실패 경험과 교훈)

**🇯🇵 回答**

デプロイ前のパフォーマンステストを、本番と異なる小規模な環境で行って、ローンチ直後にDBがダウンした経験があります。この失敗から、本番同等のデータボリュームでの検証をCI/CDに組み込むよう改善しました。

この経験から得た、もう一段深い教訓があります。「負荷テスト」と一言で言っても、目的によって種類が違うということです。

想定される通常のピーク負荷に耐えられるかを見るのがロードテスト。意図的に限界を超えてどこでどう壊れるかを見るのがストレステスト。長時間かけ続けてメモリリークのような問題を見るのがソークテストです。

当時の失敗は、実はロードテストすら本番相当のデータ量で行っていなかったことが根本原因でした。テストの「有無」ではなく「何を再現できていないテストだったか」を具体的に特定できたこと。これが再発防止に繋がりました。

**🇰🇷 답변**

배포 전 성능 테스트를 프로덕션과 다른 소규모 환경에서 해서, 런칭 직후에 DB가 다운된 경험이 있습니다. 이 실패로 프로덕션 동등한 데이터 볼륨으로의 검증을 CI/CD에 넣도록 개선했습니다.

이 경험에서 얻은, 한 단계 더 깊은 교훈이 있습니다. "부하 테스트"라고 한마디로 말해도 목적에 따라 종류가 다르다는 겁니다.

예상되는 통상 피크 부하를 견디는지 보는 게 로드 테스트. 의도적으로 한계를 넘어서 어디서 어떻게 무너지는지 보는 게 스트레스 테스트. 장시간 걸어서 메모리 누수 같은 문제를 보는 게 소크 테스트입니다.

당시의 실패는 사실 로드 테스트조차 프로덕션 상당의 데이터량으로 안 했던 게 근본 원인이었습니다. 테스트의 "유무"가 아니라 "무엇을 모사하지 않은 테스트였는가"를 구체적으로 특정할 수 있었던 것. 이게 재발 방지로 이어졌습니다.

---

### Q117. 納期厳守でコード品質と妥協した経験
(납기 준수를 위해 코드 품질과 타협한 경험)

**🇯🇵 回答**

はい、あります。市場投入スピードが最優先の場合、完璧なアーキテクチャよりも動くプロダクトを優先しました。ただし妥協しても、APIのインターフェース設計とDBスキーマだけは後から変更が困難なので妥協しませんでした。

この判断基準は、「後から変更するコストが可逆か不可逆か」という一点で切り分けています。

実装の内部ロジックやコードの綺麗さは、後からいくらでも書き直せる可逆的な決定です。一方、外部に公開するAPIの契約や、一度データが入り始めたDBスキーマは、変更した瞬間に他システムとの整合性が壊れる不可逆な決定です。

だから妥協の優先順位は「可逆なものから先に妥協して、不可逆なものは最後まで守る」という一貫した論理で決めています。そして妥協した部分は、後日リファクタリングする前提で技術的負債として記録しました。

**🇰🇷 답변**

네, 있습니다. 시장 진입 속도가 최우선인 경우, 완벽한 아키텍처보다 작동하는 프로덕트를 우선했습니다. 다만 타협하더라도 API 인터페이스 설계와 DB 스키마만큼은 나중에 바꾸기 어려우니까 타협하지 않았습니다.

이 판단 기준은 "나중에 바꾸는 비용이 가역이냐 비가역이냐"라는 한 점으로 나눕니다.

구현의 내부 로직이나 코드의 깔끔함은 나중에 얼마든지 다시 쓸 수 있는 가역적인 결정입니다. 반면 외부에 공개하는 API 계약이나, 한번 데이터가 들어오기 시작한 DB 스키마는 바꾸는 순간 다른 시스템과의 정합성이 깨지는 비가역적인 결정입니다.

그래서 타협의 우선순위는 "가역적인 것부터 먼저 타협하고, 비가역적인 건 끝까지 지킨다"는 일관된 논리로 정합니다. 그리고 타협한 부분은 나중에 리팩터링할 전제로 기술 부채로 기록했습니다.

---

### Q118. コミュニケーションが難しい部署との協業成果
(커뮤니케이션이 어려운 부서와의 협업 성과)

**🇯🇵 回答**

IT用語が通じないビジネス部門と対立した際、「なぜできないか」ではなく「どうすればできるか」を代替案として提示しました。そのうえで、それぞれのコストとROIを可視化しました。相手のKPIを理解して、同じ方向を向くパートナーとして接することで信頼を構築しました。

ここで一つ付け加えたいのは、信頼構築には「小さな約束を確実に守る」という積み重ねが不可欠だという点です。最初の提案がどれだけ論理的に正しくても、ビジネス側が技術者の言葉を全面的に信用してくれるわけではありませんから。

だから私は、最初の小さなタスクを、約束した期日より少し早く、かつ約束した品質以上で納品することを意識しています。この積み重ねがあって初めて、次の大きな技術的判断についても耳を傾けてもらえる関係が作れると考えています。

**🇰🇷 답변**

IT 용어가 안 통하는 비즈니스 부서와 대립했을 때, "왜 안 되는지"가 아니라 "어떻게 하면 가능한지"를 대안으로 제시했습니다. 그 위에서 각각의 비용과 ROI를 시각화했습니다. 상대의 KPI를 이해하고 같은 방향을 보는 파트너로서 접근해서 신뢰를 구축했습니다.

여기서 하나 덧붙이고 싶은 게, 신뢰 구축에는 "작은 약속을 확실히 지킨다"는 축적이 필수적이라는 점입니다. 첫 제안이 아무리 논리적으로 맞아도, 비즈니스 쪽이 기술자의 말을 전면적으로 믿어 주는 건 아니니까요.

그래서 저는 첫 작은 태스크를 약속한 기한보다 조금 일찍, 그리고 약속한 품질 이상으로 납품하는 걸 의식합니다. 이 축적이 있어야 비로소 다음의 큰 기술적 판단에 대해서도 귀 기울여 주는 관계가 만들어진다고 생각합니다.

---

### Q119. 深刻なインシデントの事後対応進め方【Q85の再出題・進行プロセス編】
(심각한 인시던트의 사후 대응 진행 방식 — Q85 재출제, 진행 프로세스편)

> ⚠️ Q85와 같은 테마(블레임리스)입니다. Q85에서는 "왜 블레임리스인가"를 답했으니, 여기서는 **포스트모템이라는 회의 자체를 어떻게 진행하는가**에 좁혔습니다.

**🇯🇵 回答**

障害復旧後、直ちにブレームレスなポストモーテムを実施します。誰がミスをしたかではなく、なぜシステムがそのミスを防げなかったのかを深掘りして、再発防止策をタスク化します。

進行の実務としては、まず「タイムライン」を全員で正確に再構築することから始めます。一次検知は何時何分か。誰がいつ気づいたか。応急処置は何時か。ここが曖昧なまま進むと、後半の原因分析が食い違った前提の上で行われてしまうんですよ。

次に、対応を「即時対応」と「恒久対応」に明確に分けてアクションアイテム化します。即時対応だけで満足して恒久対応が宙に浮くケースが実務では非常に多いので、恒久対応には必ず担当者と期限を付けます。

もう一点、社外のステークホルダーへの説明責任も忘れてはいけません。影響範囲・原因の概要・再発防止を、専門用語を避けて誠実に伝える設計も、シニアが主導すべき領域です。

**🇰🇷 답변**

장애 복구 후에 즉시 블레임리스 포스트모템을 실시합니다. 누가 실수했는가가 아니라, 왜 시스템이 그 실수를 못 막았는지를 깊게 파고 재발 방지책을 태스크화합니다.

진행의 실무로는, 먼저 "타임라인"을 전원이 정확하게 재구성하는 것부터 시작합니다. 최초 감지는 몇 시 몇 분인지. 누가 언제 알아챘는지. 응급 처치는 몇 시인지. 여기가 애매한 채로 진행하면, 후반의 원인 분석이 서로 다른 전제 위에서 이루어지게 되거든요.

다음으로 대응을 "즉시 대응"과 "항구 대응"으로 명확히 나눠서 액션 아이템화합니다. 즉시 대응만으로 만족하고 항구 대응이 붕 뜨는 케이스가 실무에서 아주 많으니까, 항구 대응에는 반드시 담당자와 기한을 붙입니다.

한 가지 더, 사외 이해관계자에 대한 설명 책임도 잊으면 안 됩니다. 영향 범위·원인의 개요·재발 방지를 전문 용어를 피해서 성실하게 전달하는 설계도 시니어가 주도해야 할 영역입니다.

---

# Part 19. FinOps 및 클라우드 비용 최적화

### Q120. クラウドインフラのコスト削減戦略
(클라우드 인프라 비용 절감 전략)

**🇯🇵 回答**

使用率の低いインスタンスのダウングレード、スポットインスタンスの活用、古いスナップショットのライフサイクル管理。これらを定期的に行って無駄を排除します。

これに加えて重要なのが、「計測せずに最適化しない」という大原則です。感覚でインスタンスサイズを下げると、ピーク時に性能不足を起こします。必ずメトリクスで実際の使用率分布を数週間分見てから判断します。

もう一つ、技術的な最適化と同じくらい重要なのが組織的な仕組みです。全リソースにチーム単位のタグを徹底させて、コストを「誰が」使っているかを可視化します。インフラチームだけが削減の責任を負う体制だと、実際に無駄を使っている開発チームに当事者意識が生まれませんから。

各チームが自分たちのコストに責任を持つ文化を作ること。これこそがFinOpsの本質で、スポットインスタンスのような個々の施策は、その文化があって初めて継続します。

**🇰🇷 답변**

사용률이 낮은 인스턴스 다운그레이드, 스팟 인스턴스 활용, 오래된 스냅샷의 라이프사이클 관리. 이런 걸 정기적으로 해서 낭비를 없앱니다.

여기에 더해서 중요한 게 "계측하지 않고 최적화하지 않는다"는 대원칙입니다. 감으로 인스턴스 사이즈를 낮추면 피크 때 성능 부족이 납니다. 반드시 메트릭으로 실제 사용률 분포를 몇 주치 보고 나서 판단합니다.

또 하나, 기술적 최적화만큼 중요한 게 조직적인 장치입니다. 전 리소스에 팀 단위 태그를 철저히 붙여서 비용을 "누가" 쓰고 있는지를 가시화합니다. 인프라 팀만 절감 책임을 지는 체제면, 실제로 낭비하는 개발 팀한테 당사자 의식이 안 생기니까요.

각 팀이 자기들 비용에 책임을 지는 문화를 만드는 것. 이게야말로 FinOps의 본질이고, 스팟 인스턴스 같은 개별 시책은 그 문화가 있어야 비로소 지속됩니다.

---

### Q121. AI APIのトークンコスト最適化の工夫
(AI API 토큰 비용 최적화 방법)

**🇯🇵 回答**

プロンプトの冗長な説明を削って、出力をJSONなどの厳格なフォーマットに制限して出力トークンを抑えます。またよくある質問にはセマンティックキャッシュを使ってAPI呼び出しをスキップします。

これに加えて効果が大きいのがプロバイダ側の「プロンプトキャッシュ」です。これはアプリ側が管理するセマンティックキャッシュとは別物で、システムプロンプトのように毎回同じ内容を送っている部分を、プロバイダが内部的にキャッシュしてくれる仕組みですね。RAGで長いコンテキストを毎回送る構成では、この二つの併用で削減効果が重なります。

さらに、全リクエストに最上位モデルを使わないモデルルーティングも重要です。単純な分類は軽量モデルに振り分けます。

加えて、即時性が不要な大量処理にはBatch APIを使うと通常の半額程度で処理できます。これも見落とされがちな手段です。

**🇰🇷 답변**

프롬프트의 장황한 설명을 줄이고, 출력을 JSON 같은 엄격한 포맷으로 제한해서 출력 토큰을 억제합니다. 그리고 자주 나오는 질문에는 시맨틱 캐시를 써서 API 호출을 스킵합니다.

여기에 더해서 효과가 큰 게 프로바이더 쪽의 "프롬프트 캐시"입니다. 이건 앱 쪽이 관리하는 시맨틱 캐시와는 다른 거고, 시스템 프롬프트처럼 매번 같은 내용을 보내는 부분을 프로바이더가 내부적으로 캐시해 주는 장치입니다. RAG에서 긴 컨텍스트를 매번 보내는 구성에서는 이 둘의 병용으로 절감 효과가 겹칩니다.

거기에 모든 요청에 최상위 모델을 쓰지 않는 모델 라우팅도 중요합니다. 단순 분류는 경량 모델로 배분합니다.

그리고 즉시성이 필요 없는 대량 처리에는 Batch API를 쓰면 통상의 절반 정도로 처리할 수 있습니다. 이것도 놓치기 쉬운 수단입니다.

---

### Q122. サーバーレス導入時の落とし穴
(서버리스 도입 시의 함정)

**🇯🇵 回答**

技術的にはコールドスタートによるレイテンシの増加、コスト的にはトラフィックが定常的に高い場合にコンテナより割高になる、という落とし穴があります。

もう一つ、実務で最も痛い目を見やすいのがRDBとのコネクション管理です。Lambdaは短命な実行環境が並列に大量起動されるので、トラフィックが急増するとインスタンス数だけコネクションが同時に張られて、あっという間に最大接続数を使い果たします。Q13〜18で述べたコネクションプールの話が、ここに直結するわけです。

通常のアプリケーションサーバーならプールで再利用できますが、Lambdaの実行モデルはこの前提を崩します。対策としてはRDS Proxyのような仕組みを挟むか、そもそも接続の概念がないデータストアを使うか。設計変更が必要になります。

そして「サーバーレスは運用がゼロになるのではなく、運用の種類が変わるだけ」だという認識も重要です。

**🇰🇷 답변**

기술적으로는 콜드 스타트로 인한 레이턴시 증가, 비용적으로는 트래픽이 항상 높은 경우 컨테이너보다 비싸진다는 함정이 있습니다.

또 하나, 실무에서 제일 아프게 겪기 쉬운 게 RDB와의 커넥션 관리입니다. Lambda는 단명한 실행 환경이 병렬로 대량 기동되니까, 트래픽이 급증하면 인스턴스 수만큼 커넥션이 동시에 걸려서 순식간에 최대 접속 수를 다 써 버립니다. Q13~18에서 말한 커넥션 풀 얘기가 여기로 직결되는 거죠.

일반적인 애플리케이션 서버라면 풀로 재사용할 수 있는데, Lambda의 실행 모델은 이 전제를 무너뜨립니다. 대책으로는 RDS Proxy 같은 장치를 끼우거나, 애초에 접속이라는 개념이 없는 데이터 스토어를 쓰거나. 설계 변경이 필요해집니다.

그리고 "서버리스는 운영이 제로가 되는 게 아니라 운영의 종류가 바뀌는 것뿐"이라는 인식도 중요합니다.

---

# Part 20. 시스템 설계 및 아키텍처 심화

### Q123. Kafkaで順序保証が絶対に必要な場合の設計【Q41・Q43の応用】
(Kafka에서 순서 보증이 반드시 필요한 경우의 설계 — Q41·Q43 응용)

> ⚠️ Q41·Q43에서 다룬 파티션 단위 순서 보증의 응용 문제입니다. 여기서는 파티션 키 선정이 가져오는 **부하 분산과의 트레이드오프**에 초점을 맞췄습니다.

**🇯🇵 回答**

Kafkaは同一パーティション内でのみ順序を保証します。したがって、特定の注文に関するイベントが常に同じパーティションにルーティングされるよう、エンティティのIDをパーティションキーに指定します。

ここで必ず検討しなければならないのが、キーの選定が負荷分散に与える影響です。注文IDをキーにすれば順序は完璧に保証されますが、特定の大口顧客に関連する注文が集中すると、そのパーティションだけがホットになって、他が暇なのに一部だけ詰まる偏りが生じます。「順序を保証する粒度を細かくしすぎると、負荷分散の柔軟性を失う」というトレードオフが常に存在します。

対策は、順序が必要な粒度を正確に見極めることです。「注文全体」ではなく「決済ステップだけ」順序が必要なら、より細かい粒度でキーを設計できます。

パーティションキーを設定して終わりではなく、その先のホットパーティション問題まで見据えること。これが知識と実務経験の差になります。

**🇰🇷 답변**

Kafka는 동일 파티션 안에서만 순서를 보증합니다. 그래서 특정 주문에 관한 이벤트가 항상 같은 파티션으로 라우팅되도록 엔티티의 ID를 파티션 키로 지정합니다.

여기서 반드시 검토해야 하는 게, 키 선정이 부하 분산에 주는 영향입니다. 주문 ID를 키로 하면 순서는 완벽하게 보증되지만, 특정 대형 고객에 관련된 주문이 몰리면 그 파티션만 핫해져서 다른 건 한가한데 일부만 막히는 편중이 생깁니다. "순서를 보증하는 단위를 너무 세밀하게 하면 부하 분산의 유연성을 잃는다"는 트레이드오프가 항상 존재합니다.

대책은 순서가 필요한 단위를 정확하게 가려내는 겁니다. "주문 전체"가 아니라 "결제 단계만" 순서가 필요하다면 더 세밀한 단위로 키를 설계할 수 있습니다.

파티션 키를 설정하고 끝이 아니라, 그 너머의 핫 파티션 문제까지 내다보는 것. 이게 지식과 실무 경험의 차이가 됩니다.

---

### Q124. 大規模トラフィックで分散ロックを実装する方法
(대규모 트래픽에서 분산 락을 구현하는 방법)

**🇯🇵 回答**

Redisを利用したRedlockアルゴリズムを採用します。複数のRedisノードにロックを要求して、過半数から獲得できた場合のみ処理を進めることで、単一障害点を排除します。

ただし正直に申し上げると、Redlockの安全性には議論があります。Martin Kleppmannの批判ですね。GCの一時停止やネットワーク遅延によって、あるクライアントが「まだロックを持っている」と思い込んでいる間に、実は期限が切れて別のクライアントが取得してしまう。分散ロックが本質的に「時間」に依存する以上、完全には避けられません。

だから実務では「その処理が二重実行されたときの被害の大きさ」で使い分けます。キャッシュの再生成程度ならRedlockで十分です。

しかし決済や在庫の確定では、フェンシングトークンを併用します。単調増加するトークンをロック取得時に発行して、書き込み時に新旧をDB側でチェックする仕組みですね。

**🇰🇷 답변**

Redis를 이용한 Redlock 알고리즘을 채택합니다. 여러 Redis 노드에 락을 요청해서 과반수한테서 획득했을 때만 처리를 진행함으로써 단일 장애점을 배제합니다.

다만 솔직히 말씀드리면 Redlock의 안전성에는 논쟁이 있습니다. Martin Kleppmann의 비판이죠. GC의 일시 정지나 네트워크 지연 때문에, 어떤 클라이언트가 "아직 락을 갖고 있다"고 착각하는 동안 사실은 기한이 만료돼서 다른 클라이언트가 취득해 버린다. 분산 락이 본질적으로 "시간"에 의존하는 이상 완전히는 피할 수 없습니다.

그래서 실무에서는 "그 처리가 이중 실행됐을 때의 피해 크기"로 나눠 씁니다. 캐시 재생성 정도면 Redlock으로 충분합니다.

그런데 결제나 재고 확정에서는 펜싱 토큰을 같이 씁니다. 단조 증가하는 토큰을 락 취득할 때 발행하고, 쓰기 시점에 신구를 DB 쪽에서 체크하는 장치입니다.

---

### Q125. イベントソーシングとCQRSの概念と導入注意点
(이벤트 소싱과 CQRS의 개념 및 도입 주의점)

**🇯🇵 回答**

現在の状態を保存するのではなく、状態を変更するイベントの履歴を全て保存するのがイベントソーシングです。これと相性が良いのが読み書きのDBを分離するCQRSですね。強力な監査ログが得られますが、複雑性が激増します。

実装で見落とされがちな課題が二つあります。

一つ目はパフォーマンスです。集約の現在の状態を知るには、理論上全イベントを最初から再生する必要があります。イベント数が数万件に達すると再生コストが無視できなくなるので、一定間隔でスナップショットを保存して、以後はそれ以降のイベントだけを再生する設計にします。

二つ目がイベントスキーマの進化です。保存したイベントは不変であるべきですが、要件が変わって新しいフィールドが必要になると、過去の何百万件は古いスキーマのままです。読み込む側が新旧両方に対応できるようバージョニングする設計が必要になります。

このパターンの複雑性は初期構築よりも、時間が経って要件が変わったときに牙を剥きます。

**🇰🇷 답변**

현재 상태를 저장하는 게 아니라, 상태를 변경하는 이벤트 이력을 전부 저장하는 게 이벤트 소싱입니다. 이거랑 궁합이 좋은 게 읽기쓰기 DB를 분리하는 CQRS고요. 강력한 감사 로그를 얻지만 복잡도가 확 늘어납니다.

구현에서 놓치기 쉬운 과제가 두 가지 있습니다.

첫째는 성능입니다. 애그리게이트의 현재 상태를 알려면 이론상 모든 이벤트를 처음부터 재생해야 합니다. 이벤트 수가 수만 건에 달하면 재생 비용이 무시할 수 없어지니까, 일정 간격으로 스냅샷을 저장하고 이후로는 그 이후의 이벤트만 재생하는 설계로 합니다.

둘째가 이벤트 스키마의 진화입니다. 저장한 이벤트는 불변이어야 하는데, 요건이 바뀌어서 새 필드가 필요해지면 과거의 수백만 건은 옛 스키마 그대로입니다. 읽는 쪽이 신·구 양쪽에 대응할 수 있게 버저닝하는 설계가 필요해집니다.

이 패턴의 복잡성은 초기 구축보다도 시간이 지나서 요건이 바뀌었을 때 이빨을 드러냅니다.

---

### Q126. マルチテナントSaaSのデータ分離戦略
(멀티테넌트 SaaS의 데이터 분리 전략)

**🇯🇵 回答**

要件によりますが、選択肢は三段階あります。同じテーブルにtenant_idで区別する「プールモデル」、同じDBでテナントごとにスキーマを分ける「ブリッジモデル」、テナントごとに物理DBを完全分離する「サイロモデル」。コストと分離度はこの順で反比例します。

プールモデルを選ぶ場合、最も注意すべきなのがアプリケーションのバグによるテナント間のデータ漏洩です。開発者が一つのクエリでWHERE句にtenant_idを付け忘れただけで、他テナントのデータが見えてしまう重大事故に直結します。

これを人間の注意力だけに頼らず防ぐ方法があります。PostgreSQLであればRow-Level Securityを使い、DBレベルで「このセッションのtenant_idと一致する行しか見えない」という制約を強制することを強く推奨しています。アプリのミスがあってもDBが最後の防波堤になる、多層防御の考え方ですね。

**🇰🇷 답변**

요건에 따라 다르지만 선택지는 세 단계입니다. 같은 테이블에서 tenant_id로 구분하는 "풀 모델", 같은 DB에서 테넌트마다 스키마를 나누는 "브리지 모델", 테넌트마다 물리 DB를 완전 분리하는 "사일로 모델". 비용과 격리도는 이 순서로 반비례합니다.

풀 모델을 고르는 경우, 제일 주의해야 하는 게 애플리케이션 버그로 인한 테넌트 간 데이터 유출입니다. 개발자가 쿼리 하나에서 WHERE 절에 tenant_id를 깜빡한 것만으로 다른 테넌트의 데이터가 보이는 중대 사고로 직결됩니다.

이걸 사람의 주의력에만 의존하지 않고 막는 방법이 있습니다. PostgreSQL이라면 Row-Level Security를 써서, DB 레벨에서 "이 세션의 tenant_id와 일치하는 행만 보인다"는 제약을 강제하는 걸 강하게 권장합니다. 앱의 실수가 있어도 DB가 마지막 방파제가 되는, 다층 방어의 사고방식입니다.

---

# Part 21. 코딩 테스트 / 과제 리뷰 방어

### Q127. 提出課題で最も妥協した部分
(제출 과제에서 가장 타협한 부분)

**🇯🇵 回答**

時間の制約上、キャッシュ層の実装を省略して、直接DBへクエリを投げる設計にした点です。実際のプロダクション環境であれば、Redisを導入して読み取り頻度の高いマスターデータをキャッシュします。

この質問には、何を省略したかだけでなく、次にやるべきことの優先順位も添えると評価が上がると思っています。

私の場合、キャッシュ層の次に着手するのは可観測性です。今回のコードはログが最低限のテキスト出力のみなので、構造化ログとリクエストIDの導入を優先します。その次にテストで、現在は単体テスト中心なので、TestcontainersでDBを含めた結合テストを追加します。

限られた時間の中で何を優先して、何を意図的に後回しにしたかを説明できること自体が、実務でのタスク管理能力の証明になると考えています。

**🇰🇷 답변**

시간 제약상 캐시 계층 구현을 생략하고 바로 DB에 쿼리를 날리는 설계로 한 점입니다. 실제 프로덕션 환경이면 Redis를 도입해서 읽기 빈도가 높은 마스터 데이터를 캐싱하겠습니다.

이 질문에는 뭘 생략했는지만이 아니라 다음에 해야 할 것의 우선순위도 곁들이면 평가가 올라간다고 생각합니다.

제 경우, 캐시 계층 다음으로 착수하는 건 관측 가능성입니다. 이번 코드는 로그가 최소한의 텍스트 출력뿐이라, 구조화 로그와 요청 ID 도입을 우선합니다. 그다음이 테스트로, 지금은 단위 테스트 중심이니까 Testcontainers로 DB를 포함한 통합 테스트를 추가합니다.

제한된 시간 안에서 뭘 우선하고 뭘 의도적으로 뒤로 미뤘는지를 설명할 수 있다는 것 자체가, 실무에서의 태스크 관리 능력의 증명이 된다고 생각합니다.

---

### Q128. トラフィックが100倍に増えても耐えられるか
(트래픽이 100배 늘어나도 견딜 수 있는가)

**🇯🇵 回答**

アプリケーション自体はステートレスに設計しているのでスケールアウトすれば耐えられます。ただ、ボトルネックは確実にデータベースに移行するので、Read Replicaの追加やKafkaによる非同期キューイングでDBを保護する拡張が必要です。

ただし「ステートレスに設計している」と言い切る前に、自分でも気づかないうちに状態を持たせていないかを再点検する必要があります。よくある見落としが、リクエストカウンターやレート制限をインメモリの変数で実装しているケースです。これは複数インスタンスに分散した瞬間、各インスタンスが独立したカウントを持ってしまって、意図した制御が効かなくなるんですよ。

100倍という質問には、「本当にどこにも状態を持たせていないか」を自分の実装に対して疑う姿勢を見せることです。単に「スケールアウトできます」と答えるより説得力を持つと考えています。

**🇰🇷 답변**

애플리케이션 자체는 스테이트리스로 설계했으니까 스케일 아웃하면 견딜 수 있습니다. 다만 병목은 확실히 데이터베이스로 옮겨 가니까, Read Replica 추가나 Kafka를 통한 비동기 큐잉으로 DB를 보호하는 확장이 필요합니다.

다만 "스테이트리스로 설계했다"고 단언하기 전에, 저도 모르는 사이에 상태를 갖게 하지 않았는지 재점검할 필요가 있습니다. 흔한 놓침이 요청 카운터나 레이트 리밋을 인메모리 변수로 구현한 케이스입니다. 이건 여러 인스턴스로 분산되는 순간 각 인스턴스가 독립된 카운트를 갖게 돼서 의도한 제어가 안 먹히게 되거든요.

100배라는 질문에는, "정말로 어디에도 상태를 안 갖게 했는가"를 제 구현에 대해 의심하는 자세를 보이는 겁니다. 단순히 "스케일 아웃할 수 있습니다"라고 답하는 것보다 설득력을 갖는다고 생각합니다.

---

### Q129. 外部APIとの連携部分のテスト処理
(외부 API 연동 부분의 테스트 처리)

**🇯🇵 回答**

外部依存を排除するため、Mockitoなどで外部APIの振る舞いをスタブ化しました。これでネットワーク状態に依存せず、CI環境で常に高速かつ安定してテストを実行できます。

ただしモック化には一つ危険な前提があります。「モックが実際の外部APIの振る舞いと一致している保証がない」という点です。実際のAPIが仕様変更されたのにモックだけが古い挙動のままだと、単体テストは通り続けるのに本番で連携が壊れる、という事故が起きます。

これを防ぐため、WireMockのようなHTTPレベルで模倣するツールを使った統合テストも別途用意しています。さらに可能であればPactのようなコントラクトテストを導入して、提供側と消費側が同じ契約に対してテストする仕組みにします。

単体テストのモックは「速さ」のため、統合テストは「モックが現実と乖離していないことの保証」のため。目的を分けて設計しています。

**🇰🇷 답변**

외부 의존성을 없애려고 Mockito 같은 걸로 외부 API의 동작을 스텁 처리했습니다. 이러면 네트워크 상태에 의존하지 않고 CI 환경에서 항상 빠르고 안정적으로 테스트를 실행할 수 있습니다.

다만 목에는 한 가지 위험한 전제가 있습니다. "목이 실제 외부 API의 동작과 일치한다는 보증이 없다"는 점입니다. 실제 API가 스펙 변경됐는데 목만 옛 동작 그대로면, 단위 테스트는 계속 통과하는데 프로덕션에서 연동이 깨지는 사고가 납니다.

이걸 막으려고 WireMock 같은 HTTP 레벨에서 모사하는 도구를 쓴 통합 테스트도 따로 준비합니다. 거기에 가능하면 Pact 같은 컨트랙트 테스트를 도입해서, 제공 측과 소비 측이 같은 계약에 대해 테스트하는 장치로 만듭니다.

단위 테스트의 목은 "속도"를 위해, 통합 테스트는 "목이 현실과 괴리되지 않았음의 보증"을 위해. 목적을 나눠서 설계합니다.

---

### Q130. DBトランザクション失敗時の例外処理設計
(DB 트랜잭션 실패 시의 예외 처리 설계)

**🇯🇵 回答**

RuntimeExceptionをスローして `@Transactional` による自動ロールバックをトリガーさせます。同時にコントローラー層で `@ExceptionHandler` でキャッチして、統一されたエラーレスポンスを返す設計にしました。

ここで必ず押さえておくべきSpringのデフォルト挙動の罠があります。`@Transactional` はデフォルトでは非チェック例外でのみロールバックして、チェック例外ではロールバックしません。

業務ロジックで独自の検査例外を定義してそのまま投げると、DBへの書き込みが実行された状態のままコミットされてしまう、見えにくいバグになるんですよ。変えたい場合は `rollbackFor = Exception.class` を明示する必要があります。私はチームの規約として、業務例外は非チェック例外として設計するルールを設けています。

もう一点、一時的なデッドラックのような回復可能なエラーは、Spring Retryで自動リトライさせて、それでも失敗した場合にのみエラーを返す段階を設けています。

**🇰🇷 답변**

RuntimeException을 던져서 `@Transactional`에 의한 자동 롤백을 트리거합니다. 동시에 컨트롤러 계층에서 `@ExceptionHandler`로 캐치해서 통일된 에러 응답을 반환하는 설계로 했습니다.

여기서 반드시 짚어 둬야 할 Spring의 기본 동작의 함정이 있습니다. `@Transactional`은 기본적으로 비검사 예외에서만 롤백하고, 검사 예외에서는 롤백하지 않습니다.

업무 로직에서 독자적인 검사 예외를 정의해서 그대로 던지면, DB로의 쓰기가 실행된 상태 그대로 커밋돼 버리는 눈에 잘 안 띄는 버그가 되거든요. 바꾸고 싶으면 `rollbackFor = Exception.class`를 명시해야 합니다. 저는 팀 규약으로 업무 예외는 비검사 예외로 설계한다는 룰을 두고 있습니다.

한 가지 더, 일시적인 데드락 같은 회복 가능한 에러는 Spring Retry로 자동 재시도시키고, 그래도 실패한 경우에만 에러를 반환하는 단계를 두고 있습니다.

---

# Part 22. 시니어리티, 조직 문화 및 멘토링

### Q131. 「開発が遅れてもテストコードは必ず書くべき」という意見
("개발이 늦어져도 테스트 코드는 반드시 써야 한다"는 의견)

**🇯🇵 回答**

基本的には同意します。テストのないコードは未来の技術的負債そのものだからです。ただしPoCや寿命が極端に短いキャンペーン用システムなど、ビジネス要件によってはカバー率を意図的に下げる柔軟な判断も、シニアには必要だと考えています。

この判断はQ94で述べたYAGNIの精神とも一致します。一律のルールとして扱うのではなく、「そのコードがどれだけ長く生きて、どれだけ変更され続けるか」という寿命の予測に応じて投資量を変える、という考え方です。

数年使われる基幹システムのコードと、来月には役目を終えるキャンペーンページのコードとでは、テストにかける工数の適正値は全く違いますよね。

この判断を独断で行うのではなく、「このコードは短命なので薄いテストにします。ただしこの部分だけは金銭が絡むので厚くします」と事前に共有して合意を得る。これがシニアとしての責任ある柔軟性だと考えています。

**🇰🇷 답변**

기본적으로는 동의합니다. 테스트 없는 코드는 미래의 기술 부채 그 자체니까요. 다만 PoC나 수명이 극단적으로 짧은 캠페인용 시스템처럼, 비즈니스 요건에 따라서는 커버리지를 의도적으로 낮추는 유연한 판단도 시니어한테는 필요하다고 생각합니다.

이 판단은 Q94에서 말한 YAGNI의 정신과도 일치합니다. 일률적인 룰로 다루는 게 아니라, "그 코드가 얼마나 오래 살고 얼마나 계속 변경될지"라는 수명 예측에 따라 투자량을 바꾼다는 사고방식입니다.

몇 년 쓰이는 기간 시스템의 코드와, 다음 달이면 역할을 끝내는 캠페인 페이지의 코드는 테스트에 들이는 공수의 적정치가 완전히 다르잖아요.

이 판단을 독단으로 하는 게 아니라, "이 코드는 단명하니까 테스트를 얇게 하겠습니다. 다만 이 부분만은 금전이 걸려 있으니까 두껍게 하겠습니다"라고 미리 공유하고 합의를 얻는다. 이게 시니어로서의 책임 있는 유연성이라고 생각합니다.

---

### Q132. 若手のコードの保守性が最悪だった場合のレビュー
(주니어의 코드 유지보수성이 최악일 때의 리뷰)

**🇯🇵 回答**

頭ごなしに否定せず、まずは要件を満たして動く状態にした努力を称賛します。その上で、「もしこの仕様が明日変更されたら、どこを直す必要があるか?」という問いを投げかけて、自身で保守性の低さに気づけるよう導きます。

これはQ22のコードレビュー、Q24の若手育成と一貫した手法です。答えを教えるのではなく、質問によって本人に気づかせる。「ここが良くない」と直接指摘するより、「変更が入ったらどこを触ることになるか」を想像させるほうが、その痛みを実感として持ち帰ってもらえますから。

さらに一歩進めて、気づきが得られた後にすぐ正解を提示するのではなく、まず本人に改善案を考えさせることも大事にしています。私が正解を先に言うと、本人は「次からはこう書けばいいのか」という表面的な学びで終わりがちなんですよ。自分で試行錯誤する過程を経ることで、その原則が本当の意味で根付きます。

**🇰🇷 답변**

다짜고짜 부정하지 않고, 먼저 요건을 충족해서 돌아가게 만든 노력을 칭찬합니다. 그다음에 "만약 이 사양이 내일 바뀐다면 어디를 고쳐야 할까요?"라는 질문을 던져서, 스스로 유지보수성이 낮다는 걸 깨닫게 이끕니다.

이건 Q22의 코드 리뷰, Q24의 주니어 육성과 일관된 방법입니다. 답을 알려주는 게 아니라 질문으로 본인이 알아채게 한다. "여기가 안 좋다"고 직접 지적하는 것보다, "변경이 들어오면 어디를 건드리게 될지"를 상상하게 하는 편이 그 아픔을 실감으로 가져가게 되니까요.

거기서 한 걸음 더 나아가, 깨달음을 얻은 뒤에 바로 정답을 제시하는 게 아니라 먼저 본인한테 개선안을 생각하게 하는 것도 중요하게 여깁니다. 제가 정답을 먼저 말하면 본인은 "다음부턴 이렇게 쓰면 되는구나"라는 표면적인 배움으로 끝나기 쉽거든요. 스스로 시행착오하는 과정을 거쳐야 그 원칙이 진짜 의미로 뿌리내립니다.

---

### Q133. レガシーシステムとのブリッジ設計
(레거시 시스템과의 브리지 설계)

**🇯🇵 回答**

ストラングラーフィグ・パターンを採用します。APIゲートウェイを前面に配置して、新規機能から徐々に新しいスタックにトラフィックを流して、段階的にレガシーを縮小します。

このパターンを選ぶ理由は、Q82のリファクタリングの原則と同じです。一気に書き換えるビッグバンリライトは、途中で問題が起きたときに戻す手段がなくてリスクが高い。ストラングラーフィグは常に両方が稼働している状態を経由するので、いつでもルーティングを止めてレガシーに戻せます。

実務上最も難しいのは、「いつ移行を完了とするか」の見極めと、両システムを並行維持する期間のコストです。移行が長引くと二重の負担が発生して、これ自体がプロジェクトを圧迫します。

だから導入時に、機能単位で優先順位とスケジュールを明確にして、具体的な期限を最初に合意しておきます。期限のないストラングラーフィグは、永遠に終わらない移行という別の負債になりますから。

**🇰🇷 답변**

스트랭글러 피그 패턴을 채택합니다. API 게이트웨이를 전면에 배치하고, 신규 기능부터 서서히 새로운 스택으로 트래픽을 흘려보내면서 단계적으로 레거시를 축소합니다.

이 패턴을 고르는 이유는 Q82의 리팩터링 원칙과 같습니다. 한 번에 다시 쓰는 빅뱅 리라이트는 도중에 문제가 생겼을 때 되돌릴 수단이 없어서 리스크가 높습니다. 스트랭글러 피그는 항상 둘 다 가동 중인 상태를 거치니까, 언제든 라우팅을 멈추고 레거시로 되돌릴 수 있습니다.

실무상 제일 어려운 건 "언제 이행을 완료로 볼 것인가"의 판단과, 두 시스템을 병행 유지하는 기간의 비용입니다. 이행이 길어지면 이중 부담이 생기고, 이것 자체가 프로젝트를 압박합니다.

그래서 도입할 때 기능 단위로 우선순위와 스케줄을 명확히 하고 구체적인 기한을 처음에 합의해 둡니다. 기한 없는 스트랭글러 피그는 영원히 안 끝나는 이행이라는 또 다른 부채가 되니까요.

---

### Q134. 意見と正反対のアーキテクチャが決定された場合の対応【Q9の応用・Disagree and Commit】
(의견과 정반대 아키텍처가 결정된 경우의 대응 — Q9 응용, Disagree and Commit)

> ⚠️ Q9에서 말한 판단 축의 앞에 있는, **"그래도 결정이 자기 의견과 달랐을 때의 처신"**을 묻는 질문입니다.

**🇯🇵 回答**

決定が下される前はデータと論理に基づいて徹底的に議論しますが、一度チームとして決定が下された後は、それに全力を尽くして協力します。自分の意見に固執してプロジェクトを停滞させることは、シニアとして最も避けるべき態度だからです。

これはAmazonで有名になった「Disagree and Commit」そのものですね。

ここで大事なのは、Commitが「黙って従う」ことではない点です。決定後も、実装を進める中で自分が懸念していたリスクが実際に顕在化した場合は、隠さず早期に共有します。「反対したから知らせない」のではなく「反対したからこそ、その兆候には人一倍敏感でいる」という姿勢です。

もう一つ重要なのが、自分の懸念をADRとして記録に残しておくことです。「この決定にはこういう懸念があったが、こういう理由で採用した」と書き残せば、後で問題が起きても「言わなかったこと」にはなりませんし、次の判断材料になります。

**🇰🇷 답변**

결정이 내려지기 전에는 데이터와 논리에 기반해서 철저하게 토론하지만, 일단 팀으로서 결정이 내려진 뒤에는 거기에 전력을 다해 협력합니다. 제 의견을 고집해서 프로젝트를 정체시키는 건 시니어로서 가장 피해야 할 태도니까요.

이건 Amazon에서 유명해진 "Disagree and Commit" 그 자체입니다.

여기서 중요한 건 Commit이 "묵묵히 따르는 것"이 아니라는 점입니다. 결정 후에도 구현을 진행하는 중에 제가 우려했던 리스크가 실제로 드러나면 숨기지 않고 조기에 공유합니다. "반대했으니까 안 알린다"가 아니라 "반대했기 때문에 그 징조에는 남들보다 민감하게 있는다"는 자세입니다.

또 하나 중요한 게, 제 우려를 ADR로 기록에 남겨 두는 겁니다. "이 결정에는 이런 우려가 있었지만 이런 이유로 채택했다"고 적어 두면, 나중에 문제가 생겨도 "말하지 않았던 것"이 되지 않고 다음 판단 재료가 됩니다.

---

### Q135. 若いチームへの融和(20-30代中心のチーム)
(젊은 팀과의 융화 — 20~30대 중심 팀)

**🇯🇵 回答**

年齢や過去の経歴を振りかざすのではなく、サーバントリーダーシップを徹底します。私が持つ大規模インフラのノウハウを彼らの挑戦を支える土台として提供して、同時に彼らから最新の知識を素直に学ぶ姿勢で、互いに成長できる関係を築きます。

「教える側と教わる側」という固定した上下関係ではなく、領域によって入れ替わる。この関係性を具体的な行動で示すことが重要だと考えています。私からインフラ設計のノウハウを共有する一方で、最新のフロントエンドについては若手に「これ、教えてもらえますか」と積極的に頼る場面を意図的に作ります。

年齢を重ねた技術者が陥りやすい罠は、「経験がある」ことを「常に正しい」と混同することです。過去の経験は貴重な判断材料ですが、今の文脈では通用しない場合もありますから。

年齢が上であることは貢献の一つの形であって、意思決定における優越性を意味しない。これが私の一貫した立場です。

**🇰🇷 답변**

나이나 과거 경력을 내세우는 게 아니라 서번트 리더십을 철저히 실천합니다. 제가 가진 대규모 인프라 노하우를 그들의 도전을 뒷받침하는 토대로 제공하고, 동시에 그들한테서 최신 지식을 솔직하게 배우는 자세로 서로 성장할 수 있는 관계를 만들겠습니다.

"가르치는 쪽과 배우는 쪽"이라는 고정된 상하 관계가 아니라 영역에 따라 바뀐다. 이 관계성을 구체적인 행동으로 보이는 게 중요하다고 생각합니다. 제가 인프라 설계 노하우를 공유하는 한편으로, 최신 프론트엔드에 대해서는 주니어한테 "이거 좀 알려 주실 수 있나요?"라고 적극적으로 의지하는 장면을 의도적으로 만듭니다.

나이가 있는 기술자가 빠지기 쉬운 함정은 "경험이 있다"는 걸 "항상 옳다"는 것과 혼동하는 겁니다. 과거 경험은 귀중한 판단 재료지만, 지금의 맥락에서는 안 통하는 경우도 있으니까요.

나이가 많다는 건 공헌의 한 형태일 뿐이고, 의사결정에서의 우월성을 의미하지 않는다. 이게 제 일관된 입장입니다.

---

## Part 8 (Q116~135) 요점

이 20문항을 관통하는 축은 "교과서적인 원칙(블레임리스, Disagree and Commit, 서번트 리더십)을 구체적인 행동 레벨까지 번역할 수 있는가"입니다.
특히 Q134·Q135는 일본 기업이 나이 든 외국인 시니어 후보자에게 갖기 쉬운 "유연성에 대한 불안"을,
추상론이 아니라 구체적인 행동 예시로 불식시키는 설계로 되어 있습니다.
