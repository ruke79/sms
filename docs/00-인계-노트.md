# 인계 노트 — interview-verify-lab

> **이 노트는 받은 원본 그대로 보존한다.** 아래 「현재 상태」만 앞에 덧붙였다.
> **본문의 수치와 명령은 인계 시점(52건 · H2 · 2모듈) 기준이라 지금과 다르다** — 대조표를 함께 둔다.

---

# 현재 상태 (2026-09-03 갱신 · 이전 09-01 / 08-31 / 08-26 / 08-13 / 원고 08-15)

```
$ ./scripts/random-ports.sh && docker compose up -d && ./gradlew test

- 합계 88건 | CONFIRMED 88 | REFUTED 0 | INCONCLUSIVE 0 | ERROR 0   (verify-labs)
- 합계  7건 | CONFIRMED  7 | REFUTED 0 | INCONCLUSIVE 0 | ERROR 0   (verify-labs-kafka)
- 합계 19건 | CONFIRMED 18 | REFUTED 0 | INCONCLUSIVE 1 | ERROR 0   (verify-labs-perfbook — 15 + Optimizing Java 후보 4)
- 합계 19건 | CONFIRMED 19 | REFUTED 0 | INCONCLUSIVE 0 | ERROR 0   (verify-labs-cloudnative — JDK 25 전용, 인프라 불필요)
- 합계 54건 | 전부 통과                                              (java-tutorial — 인프라 불필요)
- 합계 52건 | 전부 통과                                              (spring-tutorial — 인프라 불필요)
- 합계 58건 | 전부 통과                                              (javascript-tutorial — Node 22, 의존 0)
- JMH 벤치마크 3건                                                   (verify-labs-jmh — 수동 실행, 판정 없음)
```

**검증 케이스 133건 · 14개 분류 + 실행되는 튜토리얼 164건 + JMH 3건.** 케이스마다 독립된 테스트로 돌아
어느 것이 통과했는지 목록에서 바로 보이고, `-Dverify.only=DB-14` 로 하나만 다시 돌릴 수 있다.

> **2026-09-03 에 붙은 것** — *Optimizing Java* 1판(2018)·2판(2024) 요약 32개(`notes/optimizing-java/`·
> `notes/optimizing-java-2nd/`), JVM 용어 변천사(`notes/JVM-용어-변천사.md`), 2판 명제를 **JDK 25** 에서 검증하는
> `verify-labs-cloudnative`(19건 `CN-*`), JMH 모듈 `verify-labs-jmh`(§9-4 의 13·14 해소), perfbook 에 `PERF-08A`·
> `10D`·`11E`·`15A`. `verify-labs-cloudnative` 는 25 툴체인이 없으면 skip 된다(실패 아님). 첫 실행에서 REFUTED 가
> 5건 났고 전부 **케이스 쪽 전제가 틀린 것**이었다 — 임계값을 풀지 않고 명제·측정을 고쳤고 경위를 케이스 주석과
> 모듈 README §3 에 남겼다. 가장 배운 것은 `PERF-08A` 프로브의 할당을 **C2 탈출 분석이 없애** GC 가 한 번밖에 안 난
> 사건이다 — 책(1판 10장·2판 부록 A)이 경고한 죽은 코드 제거를 우리가 그대로 당했다.

> **INCONCLUSIVE 1 건은 고장이 아니라 설계다.** `PERF-10C`(원격 네트워크)는 실제 광역 링크가
> 있어야 시간을 단정하는 케이스라, 로컬·CI 에서는 게이트로 INCONCLUSIVE 를 남긴다.
> 루프백에서 통과시키면 그게 §1-2 가 말한 헛통과다. 자세한 것은 `docs/02` §9-6.

## 노트의 '다음에 할 일'(§6) 처리 현황

| 항목 | 상태 | 근거 |
|---|---|---|
| **(1) 로컬 실행 검증 — 최우선** | **완료** | 110건 전부 실행. REFUTED 0 (INCONCLUSIVE 1 은 `PERF-10C` 의 설계된 게이트) |
| **(2) 남은 질문 반영** | **완료** | Part 1~11 **Q1~Q200 전부 수령**해 전 범위 대조 (`docs/09`) |
| **(3) 답변 원고 수정 반영** | **완료** | 지적 **8건** 확정(9건에서 1건 철회) 후 **원고 파일에 직접 반영**. 원고가 저장소 안으로 들어와(`manuscripts/`) 더 이상 "적용은 사용자 몫"이 아니다 (`docs/10`) |

**(1) 에 대한 후기가 하나 있다.** 노트는 "손볼 가능성이 있는 곳"으로
`JPA-06`·`JPA-07`·`DB-06`·`DB-07`·`AI-03/04` 를 지목했는데, 실제로 첫 실행에서 터진 것은
**`DB-07` 하나뿐**이었고 원인은 답변이 아니라 검증 코드의 동기화 버그였다.
나머지는 수정 없이 통과했다. 다만 `DB-06` 은 한참 뒤에 다른 이유로 문제가 드러났다 —
`acquire`(대기)를 `usage` 에 섞어 재고 있었다(`docs/05` §21). **예측은 절반만 맞았다.**

## 노트 본문과 달라진 것

| 노트 (인계 시점) | 지금 |
|---|---|
| 검증 케이스 **52개** | **133건** (verify-labs 88 + verify-labs-kafka 7 + verify-labs-perfbook 19 + verify-labs-cloudnative 19) |
| 모듈 **2개** | **8개** — `verify-labs-kafka`(실물 브로커) · `verify-labs-perfbook`(책 명제) · `verify-labs-cloudnative`(2판 명제, **JDK 25**) · `verify-labs-jmh`(JMH) · `java-tutorial`(실행되는 교재 54건) · `spring-tutorial`(실행되는 교재 52건) 가 붙었다 |
| 분류 9개 | **14개** — `security` · `observability` · `perfbook` · `cloudnative` 신설, `msa`/`api` 확장 |
| **H2 인메모리** | **PostgreSQL 16 + pgvector** · 스트리밍 레플리카 · Kafka 3.9 · Redis 7 (`compose.yaml`) |
| `gradle wrapper` 선행 실행 필요 | **`gradle-wrapper.jar` 가 저장소에 있다** — 바로 `./gradlew` |
| 전건이 테스트 **하나** | 케이스마다 테스트 노드(110개) + `-Dverify.only` 로 선택 실행 |
| 검토 범위 Q31~Q115 | **Q1~Q200 전 범위** |
| 원고 지적 5건(사실 1 + 표현 4) | **8건** — 사실 3 · 실물 미재현 1 · 표현 4 (`docs/10`). 9건에서 **1건은 원문 대조로 철회**했다 |
| 문서 4개 | **13개** (`00`~`12`) + `notes/java-performance/` 14개 + `notes/optimizing-java/` 17개 + `notes/optimizing-java-2nd/` 20개 + `notes/JVM-용어-변천사.md` |
| 원고는 **저장소 밖**(채팅 첨부) | **`manuscripts/` 안에 열 벌**로 있다 — `원본/`·`수정본/`·`회화체/` + **`java-면접/`(145)·`spring-면접/`(145)** + 주제별 다섯 세트 **`python-`·`javascript-`·`db-`·`kafka-`·`kubernetes-면접/`(각 50)** + 용어 사전 `면접-용어-정의.md` |
| 원고의 일본어는 **검수한 적 없음** | **동사 539종·조사·경어를 기계 전수 검사**하고 12곳을 고쳤다 (`docs/12`) |
| 자동 실행 없음 (로컬 수동) | **GitHub Actions 3종** — CI(커밋마다 110건) · EC2 배포 · 원격 네트워크 검증 (`docs/11`) |
| 검증 대상은 **면접 원고뿐** | 원고 + **외부 기준 한 권** — *Java Performance* 의 명제를 같은 하네스로 검증한다 |
| 원고는 **읽는 것**뿐 | **`java-tutorial/`·`spring-tutorial/` 에서 실행된다** — 두 세트 290문항 중 판정 가능한 106건이 JUnit 테스트다 |

§2 의 실행 명령은 **더 이상 맞지 않는다.** 지금은 이렇다.

```bash
./scripts/random-ports.sh    # 빈 호스트 포트를 골라 .env 에 적는다 (한 번만)
docker compose up -d         # PostgreSQL + 레플리카 + Kafka + Redis
./gradlew test
```

§7 「파일 첨부 문제」도 지금은 해당이 없다 — 작업이 채팅 첨부가 아니라 **저장소 기반**으로 옮겨졌다.

## 그대로 유효한 것

**§3 핵심 설계**(`fact` / `expect` / `expectFlaky` 삼분법)는 지금도 이 랩의 전부다.
노트가 경고한 **"`expectFlaky` 를 남발하면 아무것도 검증하지 않는 문서가 된다"** 가
실제로 가장 값비싼 교훈이었다.

- `expectFlaky` 는 `verify-labs` 기준 **40곳**이다(아래 수치는 그 40곳에 대한 것이다 —
  2026-08-26 에 붙은 `verify-labs-perfbook` 은 별도이고, 그쪽 방침은 `docs/02` §9-5 에 있다).
  전수 점검해 보니
  **5곳은 '환경 의존'이 아니라 측정 하네스 결함**이었다 — 오토박싱, 워밍업 부재, 몰아서 재기,
  `acquire` 를 `usage` 에 섞어 재기, 밀리초로 잘라 `<=` 로 비교하기(`docs/05` §20~§22).
- 특히 마지막 것이 노트의 경고 그대로였다. `after <= before` 는 **둘 다 0이면 조용히 통과**한다 —
  흔들려서 INCONCLUSIVE 가 나는 것보다 나쁘다. 리포트에 아무 흔적이 남지 않기 때문이다.
- 남은 20곳은 결과를 정하는 것이 옵티마이저·근사 알고리즘·JIT/GC·락 타이밍이라 손댈 수 없다.
  이건 `expectFlaky` 가 맞는 자리다.

**§8 정직성 우선**도 그대로 지켰다. "검증한 척"을 만들지 않기 위해 **만들지 않은 것**이 둘 있다.

- **Kubernetes** — k3s 컨트롤 플레인까지는 실제로 띄웠지만 이 환경에서 파드가 뜨지 않았다.
  API 레벨만 보는 케이스를 올리면 `K8S-01 CONFIRMED` 가 찍히는데 정작 문항이 묻는
  런타임 동작은 확인하지 않은 상태가 된다(`docs/05` §18).
- **`Q160`~`Q164` LLM 품질·SSE** — 실제 모델이 있어야 의미가 생긴다(`docs/09` §7).

**2026-08-26 에 같은 원칙을 두 번 더 적용했다.**

- **책 1장을 요약하지 않았다.** 업로드 자료에 없었다. 목차만 보고 채워 넣을 수도 있었지만
  안 읽은 것을 읽은 척하는 셈이라 하지 않았고, `notes/java-performance/README.md` 머리에
  고지로 남겼다.
- **`PERF-10C` 를 루프백에서 통과시키지 않았다.** 조건이 안 맞는데 CONFIRMED 를 찍으면
  그게 §1-2 의 헛통과다. **로컬·CI 에서 영영 INCONCLUSIVE 로 남는 케이스를 일부러 만들었다** —
  "재지 못했다"가 리포트에 남는 편이 낫기 때문이다.

**Part 1~4·7~11 원문(PDF/MD)을 마저 받아 Q1~Q200 전체를 실물로 재대조했다(2026-08-13).**
그 결과 ⑤(마크다운 잔재) 의 실제 규모가 이전 집계보다 훨씬 컸다는 것이 드러났다 —
**Part 7~10(Q91~Q169) 은 79문항 전부**에 `**` 잔재가 있었다. 동시에 이전 목록의 Q57·Q68 은
원문에 없어 뺐다. 지적 건수는 8건 그대로다 — ⑤ 라는 기존 항목의 범위가 정정된 것이다(`docs/10` §C).

그리고 같은 원칙으로 **지적 한 건을 스스로 철회했다.** 원고 원문(Part 5)을 받아 보니
⑧ 이 지적한 ReDoS 예제가 **원고에 아예 없었다** — 랩이 "교과서 예제를 들었을 것"이라 가정하고
지적을 세운 것이다. 측정은 정확했지만 비교 대상이 실재하지 않았다.
케이스 코드에만 적용하던 정직성 원칙을 문서 대조에도 적용해야 한다는 뜻이다
(`docs/05` §23, `docs/02` §7-1).

## 원고가 저장소 안으로 들어왔다 (2026-08-15)

인계 시점의 가장 큰 구조적 제약은 **원고가 저장소 밖에 있다**는 것이었다. 랩은 지적만 할 수 있고
적용은 사용자 몫이었다. 이제 `manuscripts/` 에 **세 벌**이 들어와 있어서 그 제약이 없어졌다.

| 디렉터리 | 무엇인가 | 범위 |
|---|---|---|
| `manuscripts/원본/` | 받은 그대로. 아무것도 고치지 않았다 | Part 1·2·7~11 `.md` + Part 3~6 PDF 추출 `.txt` |
| `manuscripts/수정본/` | 원본 + `docs/10` 의 지적 8건·보강 9건 반영. **사실관계 기준** | Part 1~11 · Q1~Q200 |
| `manuscripts/회화체/` | 수정본을 **말투만** 회화체로 다시 쓴 것 | Part 1~11 · Q1~Q200 **완료** |

**회화체는 Q1~Q200 전부 끝났다.** 수정본과 **기술적 내용이 동일**하다 — 숫자·주장·구조·Q번호
상호 참조·재출제 ⚠️ 주석을 전부 그대로 두고, 문장을 짧게 끊고 연결어미와 한자어만 입에 붙는
말로 바꿨다. 일본어·한국어 양쪽 다 있다. 방침은 `회화체/Part1.md` 머리말에 적어 뒀고,
세 벌 비교표는 `manuscripts/README.md` 에 있다.

예외가 한 건 있다. `회화체/Part11.md` 의 Q170~190 은 굵은 글씨를 남겼다 — 원고 저자가
"굵은 부분이 다른 지원자와 차이를 만드는 지점"이라고 명시한 의도적 강조라서 지적 ⑤(마크다운
잔재)의 대상이 아니었고, 회화체에서도 건드리지 않았다. **랩이 지적한 것과 저자가 의도한 것을
구분하는 것도 정직성의 일부**라고 봤다.

## 외부 기준으로 랩 자신을 검사했다 (2026-08-26)

이 랩은 **자기 원고만 검증 대상으로 삼고 있었다.** 그러면 "무엇을 재기로 했는가"를 랩 자신이
정하게 되고, 빠뜨린 것은 빠뜨렸다는 사실조차 남지 않는다. 그래서 외부 기준을 하나 붙였다 —
Scott Oaks, *Java Performance: The Definitive Guide*.

### (1) 책을 요약했다 — `notes/java-performance/` 13개 파일

2장~12장 + 부록 A. **1장(Introduction)은 업로드 자료에 없어서 요약하지 않았다** — 안 읽은 것을
읽은 척하지 않는다는 §8 을 그대로 적용했다. 장마다 끝에 **"이 장을 우리 랩에 비춰 보면"** 표를
붙였는데, 이건 자랑이 아니라 **구멍 목록**이다.

### (2) 구멍을 전수 분류했다 — `docs/02` §9

그 표에서 "검증 케이스 없음"으로 남은 줄이 **25개**였다. 넷으로 갈랐다.

| 갈래 | 건수 | 뜻 |
|---|---|---|
| **못 하는 것** | 3 → **2** | 조건을 만들 수 없다 (32GB 힙 / JDK 6). 하나는 아래 (5) 에서 빠졌다 |
| **사라진 것** | 2 | JDK 가 없앴다 — 편향 락 기본 비활성, `AggressiveOpts` 제거, `StringTableSize` 1009→65536 |
| **별도 JVM 필요** | 3 | §8 이 이미 제외한 영역. "못 하는 것"이 아니라 **하네스를 키우기로 결정하지 않은 것** |
| **안 한 것** | 14 | **지금 이 환경에서 잴 수 있는데 안 재고 있었다** |

> **이 분류 자체가 이번 작업의 요점이다.** §1-3 「검증하지 못한 것」에는 Kubernetes 와 LLM 품질
> 둘뿐이었고 §8 은 의도적으로 뺀 것의 목록이었다. **그 사이에 14개가 있었는데 어느 목록에도
> 없었다** — 없다는 사실조차 적혀 있지 않았다. **"못 하는 것"과 "안 한 것"을 같은 칸에 넣지
> 않는 것**이 §8 정직성 원칙의 대칭이다.

### (3) "안 한 것"을 케이스로 만들었다 — `verify-labs-perfbook` 15건

`PERF-<장번호>`. 측정 원칙은 랩 공통 규칙 그대로 — **셀 수 있는 것은 결정적 `expect`,
시간 비교만 `expectFlaky`**. 그래서 지연 스트림은 시간이 아니라 람다 호출 횟수를 세고,
버퍼드 I/O 는 하부 `read` 호출 횟수(524,288 → 65)를 센다.

**만들면서 겪은 것이 §1-1 의 목록과 같은 종류였다.**

| 케이스 | 처음 결과 | 실제 원인 | 조치 |
|---|---|---|---|
| `PERF-12B` 예외 비용 | INCONCLUSIVE | 던지기·되감기까지 섞어 쟀다 — 되감기는 스택을 꺼도 깊이에 비례해 남는다 | 임계값을 풀지 않고 **측정을 명제("생성 시 수집")에 맞게 좁혔다** |
| `PERF-11C` L2 캐시 | **REFUTED** | 명제가 아니라 검증식의 전제가 틀렸다 — **`persist` 는 커밋 시점에 이미 L2 에 넣는다**(쓰기 경유) | 캐시를 비우고 시작하도록 고치고, 발견을 note 에 남겼다 |
| `PERF-09B` 암달 | INCONCLUSIVE | 작업량이 작아 스레드 생성 오버헤드가 병렬 이득을 먹었다 | 작업량을 키웠다 |

**셋 다 답변(명제)이 아니라 랩 쪽 문제였다.** REFUTED 를 보고 "책이 틀렸다"로 가지 않은 것이
중요하다 — 먼저 의심할 것은 언제나 우리 측정이다.

### (4) 자동 실행을 붙였다 — `.github/workflows/` 3종, `docs/11`

| 워크플로 | 언제 | 무엇 |
|---|---|---|
| `ci.yml` | 커밋마다 | compose 로 실물 인프라를 띄우고 110건 전부, 리포트를 아티팩트로 |
| `deploy-ec2.yml` | 수동 | EC2 프리티어에 rsync 배포 → 그 장비에서 실행 → 리포트 회수 |
| `remote-network.yml` | 수동 | 서버는 EC2 에, 측정은 러너에서 — 그 사이가 WAN |

EC2 는 프리티어(RAM 1GB)를 전제로 **스왑 2GB** 를 붙이고 빌드는 러너가 하게 했다.
**1 vCPU 에서 `PERF-09A`·`PERF-09B` 가 INCONCLUSIVE 로 남는 것은 정상 결과**라는 것도
`docs/11` §4 에 미리 적어 뒀다 — 나중에 보고 "깨졌다"고 오해하지 않도록.

### (5) 그 결과 §9-1 이 3건 → 2건이 됐다 — `PERF-10C`

"못 하는 것"에 있던 **10장 "출력을 줄여라"** 가 해소됐다. 막고 있던 것은 실제 광역 네트워크였고,
EC2 배포가 그 조건을 만들었다. **이 케이스만 방향이 반대다** — 서버를 EC2 에 두고 클라이언트를
러너에서 돌려야 그 사이가 WAN 이 된다. 인스턴스 안에서 돌리면 루프백이라 조건이 사라진다.

> **여기서도 예상이 실측과 달랐다.** "루프백이면 두 응답의 시간 차이가 사라질 것"으로 보고
> 케이스 주장에 그렇게 써 뒀는데, 재 보니 **루프백에서도 3.19배가 벌어졌다.** 남아 있는 것은
> 대역폭이 아니라 **서버가 큰 응답을 만들어 소켓에 복사하는 비용**이었다. 주장에서 그 문장을
> 빼고 note 를 "루프백에서는 링크 대역폭 성분만 사라진다"로 고쳤다.
> **§1-1 에 한 줄 더 붙는 셈인데, 이번에는 하네스 결함이 아니라 주장 문장이 과했던 경우다.**

## 원고가 다섯 벌이 됐고, 일본어를 처음으로 검사했다 (2026-08-31)

### (1) 일본어 전수 조사 — `docs/12`

원고의 일본어가 **자연스러운지 한 번도 확인한 적이 없었다.** 형태소 분석기로 기계 검사했다.

| 대상 | 규모 | 진짜 오류 |
|---|---|---|
| 동사 | 539종 · 5,995회 | **11곳** |
| 한자어+する | 425종 · 1,689회 | (위에 포함) |
| 문말 체 · 경어 · 조사 | 문말 1,881개 | **1곳** |

**확정 오류는 `リタイア`(은퇴) → `リトライ`(재시도) 하나였다.** 한국어 대응 문장이 "재시도"이고
세 줄 뒤에서는 맞게 쓰고 있었다. `원본/` 에도 있으니 원고에서 온 것이고,
**`회화체/` 는 이미 맞게 되어 있어 두 판이 어긋나 있었다** — 조사 오류(`障害を対応`)에서도
똑같은 일이 반복됐다.

> **이 조사에서 가장 중요한 결과는 오탐률 99.2% 다.** 후보 129건 중 진짜는 1건이었다.
> `〜の方が`(비교)를 바이트경어로, `集中させる`(사역)를 격지배 오류로 잡는 식이었다.
> **도구 출력을 그대로 적용했으면 멀쩡한 문장 128곳을 고쳤을 것이다.**
> §1-1 의 "판별식이 틀릴 수 있다"가 자연어 쪽에서도 그대로 성립했다.
>
> 그리고 **이 판정은 실행으로 검증할 수 없다.** 추출은 기계적이지만 자연스러움 판정은
> 의견이므로, `docs/12` §0 에 그 고지를 문서 첫 절로 박아 두었다.
> **원어민 검수를 대체하지 않는다.**

### (2) 원고 두 세트 추가 — `java-면접/` · `spring-면접/`

기존 Q1~Q200 이 아키텍처·데이터·조직까지 넓게 다루는 반면, 이 둘은 **기술 한 축씩 깊게** 판다.

| 세트 | 구성 | 특징 |
|---|---|---|
| `java-면접/` (145) | 언어기초 30 / 컬렉션 25 / 동시성 30 / JVM·GC 30 / 모던자바 30 | 일부가 `PERF-*` 케이스와 이어진다 |
| `spring-면접/` (145) | 코어·DI 30 / AOP·트랜잭션 25 / Web·MVC 30 / 데이터접근 30 / 운영·테스트·보안 30 | **`SPRING-*`·`JPA-*`·`SEC-*` 케이스가 실행 근거인 문항이 25개 이상** |

답변은 **30~40초 분량**으로 짧다. 결론부터 말하고 더 물으면 들어가는 구성이다.

**각 세트의 README 에 "무엇이 실행 근거이고 무엇이 아닌지"를 표로 갈라 적었다.**
면접에서 수치를 인용할 때 직접 잰 것과 읽은 것을 구분해 말하기 위해서다 —
되물었을 때 근거를 못 대면 오히려 마이너스가 된다. 이것도 §8 정직성의 연장이다.

**시간이 부족할 때를 대비해 `필수-키노트.md` 를 각 세트에 두었다.**
S급(반드시 나온다)·A급(자주 나온다)으로 등급을 매기고, 문항마다 **일본어 한 문장**을
그대로 외울 수 있게 정리한 것이다.

### (3) 새 원고에도 같은 검사를 돌렸다

`docs/12` 의 검사를 두 세트에 그대로 적용했다. **`spring-면접/Part1` 에서
`리クエスト`(한글 + 가타카나 혼입)를 잡았다** — Java 세트에는 없던 유형이라
**한글 혼입 검사를 새로 만들고 나서야** 발견됐다. 검사 항목이 하나 늘었다.

## 원고를 실행 가능한 교재로 바꿨다 — `java-tutorial/` · `spring-tutorial/` (2026-09-01)

`java-면접/`·`spring-면접/` 의 문항은 그 자체로는 **검증되지 않은 문장 묶음**이다.
그중 **실행으로 판정 가능한 것**을 골라 레슨으로 옮긴 것이 이 두 모듈이다 —
자바 쪽은 순수 자바만으로, 스프링 쪽은 인프라 없이(DB·브로커·톰캣 불요) 판정되는 것만.
**레슨 하나가 JUnit 테스트 하나**라서 `./gradlew :java-tutorial:test` /
`./gradlew :spring-tutorial:test` 로 문장이 실제로 성립하는지 확인할 수 있다.
자바 **7레슨 54건** + 스프링 **7레슨 52건**, 전부 통과(JDK 17.0.19 / Boot 3.3.5).

| 레슨 | 주제 | 건수 | 대응 문항 |
|---|---|---|---|
| 1 | `==`·`equals`·`hashCode` 계약 | 7 | Q1·Q2·Q8·Q25 |
| 2 | 문자열과 불변 | 7 | Q3~Q5·Q9·Q16·Q47 |
| 3 | 예외 — 삼키지 않기 | 7 | Q18~Q20·Q115·Q143 |
| 4 | 컬렉션 — `HashMap` 의 안쪽 | 10 | Q35~Q55 |
| 5 | 동시성 — 무엇이 보장되고 무엇이 아닌가 | 7 | Q39·Q40·Q60·Q61·Q65·Q75 |
| 6 | 스레드풀 — 상한을 정하는 일 | 8 | Q57·Q67·Q70~Q74 |
| 7 | 모던 자바 — 스트림과 Optional | 8 | Q118~Q126 |

`spring-tutorial/` (문항 번호는 `spring-면접/` 기준):

| 레슨 | 주제 | 건수 | 대응 문항 |
|---|---|---|---|
| 1 | DI 컨테이너 | 8 | Q3~Q5·Q10·Q28 |
| 2 | 스코프와 생명주기 | 7 | Q6~Q8·Q13 |
| 3 | 프록시 | 7 | Q33~Q36·Q47·Q52·Q53 |
| 4 | 트랜잭션 경계 | 8 | Q35·Q37~Q39·Q42·Q43·Q49 |
| 5 | Web MVC | 8 | Q57·Q58·Q60~Q66 |
| 6 | 설정 바인딩 | 7 | Q18~Q20·Q144 |
| 7 | 캐시·이벤트·비동기 | 7 | Q23·Q48·Q49·Q79·Q104 |

**스프링 쪽의 설계 결정 둘:**

- **인프라 제로.** DB·브로커·톰캣 없이 돈다. 트랜잭션 레슨(4)은 DataSource 대신
  **BEGIN/COMMIT/ROLLBACK 을 기록하는 트랜잭션 매니저**를 꽂았다 — 전파·롤백 규칙의
  판단 로직은 스프링의 `AbstractPlatformTransactionManager` 실물이 하므로 흉내가 아니다.
  단 이것은 '스프링의 경계 결정'까지다. DB 의미론은 `verify-labs` 소관으로 명시했다.
- **자기 호출 함정을 한 번이 아니라 세 번 보여 준다** — AOP(3-2) → @Transactional(4-6)
  → @Cacheable(7-2). 같은 원인이 다른 기능에서 재발하는 것을 실행으로 잇는 구성이다.

### (스프링 쪽) 만들다 여섯 번 틀렸고, 하나는 레슨이 됐다

자세한 목록은 `spring-tutorial/README.md` §4. 넷은 레슨·주석으로
승격했다: ①`ApplicationContextRunner` 는 이름과 달리 **Boot 기본값을 적용**한다(→레슨 1-8),
②기록용 트랜잭션 매니저에 `SmartTransactionObject` 가 없으면 rollback-only 전염(4-7)이
재현되지 않는다, ③레슨 3-5 에 써 놓은 'CGLIB 프록시 필드는 비어 있다'를 **레슨 7 에서
내가 그대로 밟았다**(필드 직접 접근 → NPE), ④`@EnableTransactionManagement` 가 없으면
`@TransactionalEventListener` 가 **조용히 일반 리스너로 강등**된다(7-5). 나머지 둘은
Boot 가 평소 대신해 주던 것들이다 — Spring 6.1 의 `-parameters` 요구,
`StringHttpMessageConverter` 의 ISO-8859-1 기본값.

**선정 기준은 두 모듈 모두 커버리지가 아니라 판정 가능성이다.** 290문항 중 106개만
레슨이 됐고(자바 54/145 · 스프링 52/145), 나머지는 실행으로 판정할 수 없거나
(설계 논의·용어 설명) 모듈의 범위를 벗어난다 — DB 가 있어야 성립하는 스프링 문항
(Part 4 대부분)은 `verify-labs` 의 `SPRING-*`·`JPA-*`·`DB-*` 가, 시간 측정은
`verify-labs-perfbook` 이 실행 근거다. 의존은 자바 쪽이 JUnit·AssertJ 둘,
스프링 쪽이 스타터 넷(web·aop·cache·validation)+spring-tx 뿐이다.

### `observe` — 이 모듈판 `expectFlaky`

`Lesson` 클래스의 도구 셋 중 **`observe` 는 환경에 좌우되는 값을 출력만 하고
단정하지 않는다.** 저장소의 `expect`/`expectFlaky` 구분과 같은 발상이다(§3).
`java-tutorial` 에 5곳(레슨 5 에 4, 레슨 2 에 1). **테스트가 초록이라고 해서 그 줄까지
증명된 것은 아니라는 뜻**이고, README §5 에 그렇게 적어 뒀다.
`spring-tutorial` 은 0곳이다 — 다루는 명제가 전부 결정적이어서 쓸 일이 없었을 뿐,
원칙이 다른 게 아니다(그 사실도 저쪽 README §5 에 적었다).

레슨 5-1 이 대표적이다. 단정한 것은 「`AtomicInteger` 는 정확하다」와
「일반 `int` 는 기대값을 넘지 못한다」 둘뿐이고, **「반드시 갱신 손실이 난다」는
단정하지 않았다** — 스케줄링은 우리가 정하지 못하기 때문이다.
5-6 의 **반대 순서 데드락은 아예 재현하지 않았다**(재현하면 테스트가 영원히 멈춘다).
안 한 것이고, 안 했다고 README 에 적었다.

### (자바 쪽) 만들다 두 번 틀렸고, 한 번은 레슨이 됐다

- **레슨 4** — `["a","b","c"]` 에서 `"b"`(끝에서 두 번째)를 지우게 짰더니 CME 가
  나지 않아 테스트가 실패했다. `remove` 후 `size=2, cursor=2` 라 `hasNext()` 가
  먼저 false 가 되어 `next()` 의 변경 검사를 지나간다. 조건을 `"a"` 로 고치는 데서
  끝내지 않고, **밟은 함정 자체를 `4-4b`「fail-fast 는 보장이 아니다」라는
  새 레슨으로 남겼다.** 실패가 교재가 된 사례다.
- **레슨 6-2** — `remainingCapacity()` 가 `Integer.MAX_VALUE` 를 돌려줄 거라
  단정했는데 실제로는 `용량 - 현재 크기`(2,147,482,647)였다. 자바의 놀라움이 아니라
  **내 주장이 틀린 것**이라, 주장을 `쌓인 것 + 남은 것 == Integer.MAX_VALUE` 로
  고치고 **틀렸던 사실을 코드 주석에 남겼다.**

### (공통) 시간을 재지 않은 이유

자바 3-6(예외 비용)·7-2(지연 순회)는 성능 이야기지만 **시간이 아니라 호출 횟수로 쟀고**,
스프링 쪽도 마찬가지다 — 캐시(7-1~7-3)는 실제 실행 횟수를, 트랜잭션(레슨 4)은
BEGIN/COMMIT/ROLLBACK 기록의 순서를 센다. 횟수와 순서는 장비와 무관하게 결정적이라
단정할 수 있다. 시간으로 쟀다면 `docs/02` §1-2 의 해상도·최소 마진 규칙에 걸려
전부 `observe` 로 내려야 했을 것이다.
**시간 기반 검증은 `verify-labs-perfbook` 의 `PERF-12B`·`PERF-12C` 쪽 소관이다.**

## 자바스크립트도 실행 가능한 교재가 됐다 — `javascript-tutorial/` (2026-09-06)

`javascript-면접/README.md` 에는 "**이 저장소에는 자바스크립트 실행 검증 모듈이 없다**"고 적혀 있었다.
그 문장을 지우려고 만든 모듈이다. `java-tutorial` 과 같은 발상 — **레슨 하나가 테스트 하나**,
`fact`/`observe`/`lesson` 도 그대로. 다른 점은 **JVM 이 아니라 Node 22** 이고, 내장 러너(`node --test`)와
`node:assert` 만 써서 **npm 의존이 0** 이라는 것. `cd javascript-tutorial && npm test` 로 돈다.
**8레슨 58건, 전부 통과**(Node 22.22.2 · 10회 반복 0실패). CI 에는 `js-tutorial` 잡으로 붙였다.

| 레슨 | 주제 | 건수 | 대응 문항 |
|---|---|---|---|
| 1 | 스코프·클로저·`this` — `bind` 는 한 번만, 떼어낸 메서드는 `this` 를 잃는다 | 8 | Q1·Q3·Q4 |
| 2 | 프로토타입 — `class` 의 속, **프로토타입 오염을 실제로 재현** | 7 | Q5·Q11·Q15·Q38 |
| 3 | 이벤트 루프 — 마이크로태스크 선점, `nextTick` 의 진짜 규칙, `setImmediate` 의 단계 | 7 | Q6·Q32·Q33 |
| 4 | Promise — `return` 누락, **`await` 누락은 프로세스가 죽는다**, `all` 은 취소하지 않는다, 동시 수 제한 | 9 | Q7~Q10 |
| 5 | 값 — 얕은 복사, JSON 왕복의 손실, `sort()` 문자열 비교, `??`, 64비트 ID 손실 | 8 | Q2·Q11·Q12·Q14·Q20 |
| 6 | 이터레이터·제너레이터·모듈 — `break` 가 `finally` 를 부른다, **ESM 라이브 바인딩 vs CJS 값 복사** | 6 | Q16·Q17 |
| 7 | 타이머·메모리·취소 — 가짜 시계로 디바운스/스로틀, 리스너 누수, `WeakRef`, **`AbortController` 로 실제 취소** | 6 | Q8·Q10·Q18·Q19 |
| 8 | 스트림·백프레셔·워커 — `write()` 의 `false`, `pipeline` 의 상한, 청크 경계, **워커가 도는 동안 루프가 산다** | 7 | Q17·Q33·Q34·Q36 |

### 만들다 다섯 번 틀렸고, 전부 레슨이나 주석이 됐다

| 어디 | 틀린 단정 | 실제 | 처리 |
|---|---|---|---|
| 3-4 | `process.nextTick` 이 항상 마이크로태스크보다 먼저 | 테스트 본문이 이미 마이크로태스크 안이라 **맨 뒤** | 출발점(매크로/마이크로)을 나눠 두 경우 모두 단정하는 레슨으로 |
| 4-5 | 같은 프로세스에서 `unhandledRejection` 리스너로 잡는다 | `node:test` 러너가 같은 이벤트로 테스트를 실패 처리 | 자식 프로세스의 **종료 코드 1** 로 확인 — Node 15+ 기본 크래시가 그대로 보인다 |
| 8-2 | `_write` 안에서 잰 버퍼 최대치 = 200 | **199** — 두 번째 `_write` 땐 첫 청크가 이미 빠짐 | 루프 직후 `writableLength`(200)로 단정, 최대치는 기록만 |
| 8-3 | 에러 전까지 `['A','B']` 도착 | **`['A']`** — `pipeline` 이 sink 를 destroy 하며 버퍼째 버림 | "에러 시 버퍼는 사라진다"를 레슨 문장에 추가 |
| 8-7 | `await setImmediate` 뒤면 타이머가 돌았을 것 | 8-6 이 poll 단계(워커 message)에서 끝나 **같은 바퀴 check 단계**가 먼저 | 타이머로 기다리게 고침 — **3-6 에서 단정한 규칙에 내가 걸렸다** |

### `observe` 5곳 — 이 모듈에서 단정하지 않은 것

3-6(메인에서의 `setTimeout` vs `setImmediate` 순서) · 3-7(I/O 완료 순서) · 7-4(GC 뒤 `WeakRef.deref()`) ·
7-5(`FinalizationRegistry` 콜백 도착) · 8-6(워커가 도는 동안 메인 타이머 **횟수**). GC 두 건은 `--expose-gc` 로
강제해도 사양이 "회수된다"를 약속하지 않아서고, 단정한 것은 그 반대(강한 참조가 있으면 절대 회수되지 않는다)뿐이다.

### 시간을 재지 않은 것은 자바와 같다

병행/직렬은 **동시에 떠 있던 최대 개수**(4-7 은 1 대 5, 4-8 은 정확히 2), 지연 평가는 **호출 횟수**(6-1·6-4),
백프레셔는 **버퍼에 쌓인 개수**(8-1·8-2), 디바운스/스로틀은 **가짜 타이머로 시계를 돌려서** 잰다.
유일하게 타이밍에 기대는 것이  8-6 의 "워커가 도는 동안 타이머 횟수가 0 이 아니다"인데, 횟수 자체는 `observe` 했고
0/비0 판정만 단정했다. `javascript-tutorial/README.md` §5 에 적었다.

### 안 다룬 것

브라우저가 필요한 문항 전부(렌더링 Q26·DOM 이벤트 Q23·CORS 의 브라우저 쪽 Q30·Web Worker Q43), TypeScript(Q21·22),
설계·경험 문항(Q35·37·39·44~50). 50문항 중 **24문항**이 레슨에 걸렸다 — 커버리지가 아니라 판정 가능성으로 골랐다.

## 키워드 시트 — 여덟 세트 740문항을 세트당 한 장에 (2026-09-06)

사용자가 Q1~Q200 의 **키워드 시트**(문항마다 `| Q | 질문 | 키워드 |` 한 줄, 굵은 글씨 = 차별화 포인트,
🔴 = A급, 함정 표, A급 60 목록)를 만들어 보내며 등록과 함께 "다른 세트도 같은 형식으로"를 요청했다.
`manuscripts/키워드-시트/` 에 여덟 장이 있다.

| 시트 | 문항 | 출처 | 🔴 | ✅ / ▶ |
|---|---|---|---|---|
| `Q1-Q200.md` | 200 | **사용자 제공, 그대로 등록** | 원저자 A급 60 | — |
| `java.md` · `spring.md` | 각 145 | 각 세트 한국어 답변에서 뽑음 | 키노트 S급 18 | ▶ 레슨 · spring ✅ 11 |
| `python`·`javascript`·`db`·`kafka`·`kubernetes` | 각 50 | 같음 | 키노트 S급 10 | db ✅ 22 · kafka ✅ 12 · js ▶ |

**지킨 것** — (1) 키워드는 답변에 **있는 말**만 줄였고 보태지 않았다. (2) 🔴 는 각 세트 `필수-키노트` 의
S급과 정확히 일치시켰다(Q1-Q200 만 원저자 기준이라 다르고, 그 사실을 시트 README 에 적었다). (3) ✅ 는
키노트 §5·세트 README 의 대응표에 있는 케이스 id 만 붙였다 — Python·Kubernetes 시트에 ✅ 가 0 인 것은
검증하지 않았기 때문이다. (4) `Q1-Q200.md` 는 문장 단위로 재검증하지 않았음을 명시했다.

**CI** — `scripts/check-keyword-sheets.py` 를 `docs` 잡에 붙였다. 시트는 생성물이 아니라 재생성 비교가 안 되므로
**누락 · 유령 번호 · 허구 근거 id** 세 가지만 본다(원문 `### Qn.` ↔ 시트 `| n |`, ✅ id ↔ `VerificationCase.id()`
131개). 첫 실행 누락 0 · 유령 0 · 허구 0. **키워드가 답변을 잘 요약했는지는 기계가 못 본다**고 스크립트 머리말과
`docs/11` §1-4 에 적었다 — 검사가 하는 일보다 크게 말하지 않기 위해서다.

## 말로 이해하기 어려운 표현을 740문항에서 걷어냈다 (2026-09-06)

사용자가 Spring Q43 을 두고 "대답이 좀 어려운 것 같다"고 했다. 보니 조건이 `내側이 REQUIRED 로
참여했고` 라는 **용어 안에 숨어** 있었다. 글로는 맞는데 말하면 멈춘다. 같은 결함을 전부 찾았다.

`scripts/check-speech-clarity.py` 로 8,679문장을 훑어 다섯 범주(전자/후자 · 이쪽/그쪽 · 긴 문장 ·
나열 · 조건절 겹침)의 후보를 뽑고, 사람이 읽어 **47문항**을 고쳤다. 전문은
[`docs/13`](13-말로-이해하기-어려운-표현-조사.md).

**지킨 것** — (1) 사실관계를 바꾸지 않았다. 문장을 나누고 가리키는 말을 이름으로 바꿨을 뿐이다.
(2) `수정본/` 은 건드리지 않았다(말하기용이 아니라 사실관계 기준이다). (3) **분량을 늘리지 않았다** —
Q43 첫 판이 249자로 세트에서 두 번째로 긴 답변이 되어 되돌려 229자로 맞췄다. 쉽게 만들자고 길어지면 손해다.

**전자/후자 21건만 예외 없이 전부 고쳤다.** 글에서는 눈으로 한 줄 위를 보면 되지만 말에서는 되짚을
방법이 없고, 이름을 부르는 비용이 두세 글자뿐이다. 반대로 90자가 넘어도 `A는 X, B는 Y` 병렬 나열은
그대로 뒀다.

**이 검사는 CI 에 넣지 않았다.** 나열 32건 중 28건, 이쪽/그쪽 40건 중 14건이 오탐이고 그 오탐이
정당한 문장이다. 실패로 처리하면 통과시키려고 멀쩡한 문장을 망친다 — `docs/12` §10 과 같은 판단이다.
스크립트는 건수만 출력하고 종료 코드로 판정하지 않는다.

**주제별 다섯 세트에서는 거의 안 나왔다**(각 1~2건). 한 문항 30~40초를 전제로 짧게 쓴 것들이다.
길이를 먼저 정해 두면 이 결함이 잘 안 생긴다는 뜻으로 읽힌다.

## 이어서 할 일

1. ~~`docs/10-원고-수정-지시서.md` 의 8건을 실제 원고에 반영~~ — **완료(2026-08-13).**
   Part 1~11 원고 전체에 A→C→B→D 순서대로 직접 반영해 사용자에게 파일로 전달했다.
   Part 1·2·7·8·9·10·11 은 원본이 markdown 이라 그 파일을 그대로 고쳤고, Part 3·4·5·6 은
   PDF 원본이라 편집이 불가능해 추출 텍스트로 markdown 을 다시 만든 뒤 반영했다(내용은
   동일하지만 줄바꿈 지점의 띄어쓰기가 일부 소실됐을 수 있다고 파일마다 고지해 뒀다).
   반영하면서 Part 9 Q138 에서 Q69 와 같은 오역("농담(濃淡)")을 하나 더 발견해 함께 고쳤다
   (`docs/04` §2). 이후 원고 세 벌을 **저장소에 등록**하고(`manuscripts/`),
   **회화체 판을 Q1~Q200 전부 작성**했다 — **완료(2026-08-15).**
2. **Kubernetes** — 파드를 띄울 수 있는 도커 호스트만 있으면 `verify-labs-kafka` 처럼
   별도 모듈로 붙이면 된다. 막힌 것은 설계가 아니라 실행 환경이다.
3. **`Q160`~`Q164`** — 실제 모델을 붙일 수 있게 되면.
4. **`PERF-10C` 를 실제 WAN 에서 한 번 돌려 CONFIRMED 를 받는다.** 코드·워크플로는 다 됐고
   남은 것은 **EC2 인스턴스 생성 + 시크릿 3개(`EC2_HOST`/`EC2_USER`/`EC2_SSH_KEY`) 등록 +
   보안 그룹 8080 인바운드**뿐이다(`docs/11` §2·§5). **이걸 하기 전까지 이 케이스는
   INCONCLUSIVE 이고, 그 상태를 감추지 않는 것이 맞다.**
5. ~~**`docs/02` §9-4 에서 아직 안 한 것 3건** — 문장 풀의 **힙 소비** 부분, 그리고
   `final`·람다("차이가 없다"는 명제라 벽시계로는 확정 불가, **JMH 가 필요**하다 — §4 의 한계).
   JMH 를 들이면 §4 「벤치마크가 아니다」의 전제가 바뀌므로 **별도 모듈로 분리해야 한다.**~~ —
   **`final`·람다는 완료(2026-09-03)**, `verify-labs-jmh` 로 분리해 차이 없음을 확인했다(`docs/02` §9-5 추기).
   **힙 소비 부분만 남았다.**
5-1. **`verify-labs-cloudnative` 의 📄 항목** — cgroup 실물 한계(권한 필요), AOT 캐시 기동 시간(조용한 장비 필요),
   구조적 동시성(정식이 되면 `CN-13D` 로). 25 다음 LTS 가 나오면 `CN-*` 를 그 버전으로 다시 돌리는 것이 첫 할 일이다 —
   REFUTED 가 나는 곳이 곧 "또 바뀐 것"이다.
6. **`docs/02` §9-1 에 남은 2건** — 32GB 힙(프리티어로도 불가, 유료 인스턴스면 가능)과
   JDK 6 클래스로더(비교군 자체가 없다). **후자는 영구히 "못 하는 것"으로 두는 게 맞다.**
7. **일본어 원어민 검수** — `docs/12` 는 검수를 *대체*하는 문서가 아니라 **받기 위한 목록**이다.
   특히 **は/が 의 선택은 기계로 판정할 수 없어 아예 검사하지 못했다.**
   원고에서 가장 미검증인 영역이므로, 검수를 받는다면 그쪽부터 보는 것이 맞다.

---

# (이하 원본)

> 새 채팅에 이 파일과 `interview-verify-lab.zip` 을 함께 올리고
> "이 노트대로 이어서 작업해 줘" 라고 하면 그대로 이어집니다.
>
> — 인계 시점의 안내다. 지금은 저장소를 그대로 열면 되므로 zip 을 올릴 필요가 없다.

---

## 1. 지금까지 한 일 (한 줄 요약)

시니어 개발자 면접 답변 200문항 중 **Q31~Q115(85문항)** 를 검토해서,
그 답변이 실제로 맞는지 **코드로 실행해 판정하는 Spring Boot 검증 랩**을 만들었다.
검증 케이스 **52개**, 그 과정에서 **답변 원고의 사실 오류 1건 + 표현 오류 4건**을 발견했다.

---

## 2. 산출물

`interview-verify-lab.zip` — Gradle 8.14.3 / Java 17 / Spring Boot 3.3.5, 2모듈 구성

```
interview-verify-lab/
├── verify-core/     # 이식용 하네스 (의존성: spring-boot-starter 뿐)
│   └── VerificationCase / Evidence / Registry / Runner / Report / 자동설정 / REST
├── verify-labs/     # 실제 검증 케이스 52개
│   └── spring(7) jpa(7) concurrency(5) jvm(6) db(7) msa(5) resilience(7) api(2) ai(6)
└── docs/
    ├── 01-질문-검증-매핑.md          # Q31~Q115 를 A/B/C 로 분류한 표
    ├── 02-정직한-고지.md             # 이 랩이 증명 못 하는 것
    ├── 03-새-케이스-추가-가이드.md
    └── 04-답변-원고-검토-지적사항.md  # ★ 답변 수정이 필요한 부분
```

**실행**

```bash
gradle wrapper --gradle-version 8.14.3   # 최초 1회 (jar 는 zip 에 없음)
./gradlew :verify-labs:test              # → verify-labs/build/reports/verification.md
```

---

## 3. 핵심 설계 (새 케이스를 추가할 때 반드시 지킬 것)

증거를 **세 종류로 구분**하는 것이 이 랩의 전부다.

| 메서드 | 의미 | 깨졌을 때 |
|---|---|---|
| `evidence.fact(k, v)` | 판정 무관 관측값 | 리포트에 기록만 |
| `evidence.expect(설명, 조건)` | 반드시 성립할 명제 | `REFUTED` = **답변을 고쳐야 함** |
| `evidence.expectFlaky(설명, 조건)` | JIT/GC/옵티마이저 의존 | `INCONCLUSIVE` (테스트 실패 아님) |

- `expect` 를 남발하면 테스트가 자주 깨져 신뢰를 잃고,
  `expectFlaky` 를 남발하면 아무것도 검증하지 않는 문서가 된다. **이 경계가 핵심.**
- 환경 의존 케이스는 `nondeterministic()` 을 `true` 로 둔다.
- 케이스는 `VerificationCase` 를 상속한 `@Component` 하나면 끝. 자동 수집된다.
- 케이스는 **자기 뒷정리**를 한다(임시 테이블 DROP, 캐시 clear, 스레드 풀 shutdown, 데몬 스레드 + 타임아웃).

---

## 4. 질문 분류 결과 (Q31~Q115)

| 분류 | 문항 | 처리 |
|---|---|---|
| **A. 실행 검증 가능** | 41 | 케이스로 구현 완료 |
| **B. 이론 전용** | 27 | 만들지 않음 (조직·판단·설명형이라 참/거짓이 안 갈림) |
| **C. 인프라 필요** | 17 | 확장 후보로 문서화만 (Kafka/PostgreSQL/K8s/LLM/GPU) |

상세 표는 `docs/01-질문-검증-매핑.md`.

**대표 케이스 몇 개**

- `DB-06` — Q18·Q44·Q68 로 **세 번 출제된** 커넥션 풀 고갈. 누수 / 장시간 점유 / 용량 부족
  3시나리오를 실제 Hikari 풀에 걸고 `active`·`pending`·`usage` 표본으로 기계적 구분. 가장 공들인 케이스.
- `JPA-06` — Q99 "fetch join + 페이징은 메모리 처리". Hibernate `StatementInspector` 로
  **발행 SQL 에 limit 절이 없다**는 것을 문자열로 포착.
- `MSA-01` — Q31 Outbox. 커밋 후 발행 실패 시 이벤트 영구 유실 vs 릴레이 복구.
- `RES-04` — Q34 서킷브레이커. 직접 구현하지 않고 **resilience4j 실물**로 최소 시행 횟수 유무를 비교.
- `AI-04` — Q113. int8 양자화는 메모리 1/4·재현율 유지, **마트료시카 학습이 안 된 임베딩의 단순 절단은 붕괴**.

---

## 5. ★ 답변 원고에서 발견한 수정 사항

### 사실 오류 (반드시 고칠 것)

**Q64 — Base62 7자리 용량이 100배 틀렸다**

- 원고: 「7桁のBase62でおよそ350億通り」 / "7자리 Base62로 약 350억 가지"
- 실제: 62⁷ = **3,521,614,606,208 ≈ 3.5조**
- 350억대에 해당하는 것은 **6자리** (62⁶ = 568억)
- → 케이스 `API-02` 가 이 계산을 매번 검증한다.

수정 문장 예:
> 7자리 Base62로 약 3.5조 가지를 커버할 수 있으니 당분간 규모에는 충분합니다.

### 표현 오류 (발화 스크립트로 못 쓰는 상태)

| Q | 문제 | 수정 |
|---|---|---|
| Q69 | "농담(濃淡)을 두는 것이" — 일본어 濃淡 직역, 한국어로는 joke 로 읽힘 | "경중을 두는" / "강약을 조절하는" |
| Q77 | "시차가 있거나(혹은 애초에 얻을 수 없습니다)." 술어 깨짐 | "시차가 있고, 애초에 얻을 수 없는 경우도 있습니다" |
| Q78 | 발화 스크립트에 `$O(N)$` LaTeX 잔재 | "건수에 정비례합니다. 100만 건이면 100만 번 계산합니다" |
| 다수 | Q49·Q57·Q64·Q76·Q84·Q107·Q114 등에 `**「...」**` 마크다운 잔재 | 전부 제거 |

### 보강 제안 (틀리진 않았지만 한 줄 더 넣으면 강해짐)

- Q99: "Hibernate 가 HHH90003004 경고를 남기지만 예외가 아니라 놓치기 쉽습니다"
- Q100: "Kafka 3.0 부터 `enable.idempotence` 는 기본 true 입니다"
- Q105: "Argon2 를 못 쓰는 환경이면 bcrypt work factor 를 하드웨어에 맞춰 올립니다"
- Q34 / Q62: 실제 숫자로 말하기 (`minimumNumberOfCalls 5, 실패율 50%` / `제한 10/초면 경계 200ms 에 20건`)
- Q113: "마트료시카는 그렇게 학습된 모델에서만 성립합니다"

상세는 `docs/04-답변-원고-검토-지적사항.md`.

---

## 6. 아직 안 된 것 / 다음에 할 일

### (1) 로컬 실행 검증 — **최우선**

이 랩은 **컴파일·실행되지 않았다.** Claude 환경에 네트워크가 없어 의존성을 받을 수 없었다.
문법·임포트·Spring 설정 규약은 검토했지만 실제 결과는 확인이 필요하다.

→ 로컬에서 `./gradlew :verify-labs:test` 돌린 뒤 **실패 로그를 붙여 주면** 수정한다.
   특히 손볼 가능성이 있는 곳: `JPA-06`(StatementInspector 설정), `JPA-07`(H2 락 타임아웃),
   `DB-06`(HikariPoolMXBean 타이밍), `DB-07`(H2 데드락 감지), `AI-03/04`(힙 사용량).

### (2) 남은 질문 반영

- **Part 1·2 (Q1~Q30)** — 미수령. Q5(B+Tree), Q8, Q9(ADR), Q14(SQL 인젝션), Q18(커넥션 누수),
  Q19(SameSite), Q24, Q26 이 뒤 문서에서 참조되고 있다.
  ※ Q5·Q14·Q18 은 이미 만든 `DB-03`·`DB-04`·`DB-06` 으로 대부분 커버됨.
- **Q116~Q200** — 미수령.

### (3) 답변 원고 수정 반영

§5 의 사실 오류·표현 오류를 실제 스크립트 파일에 반영.

---

## 7. 파일 첨부 문제 (다음 채팅에서 주의)

한 채팅에 PDF 를 6개째 올리면 "이 채팅의 첨부 상한에 도달" 에러가 난다.
개수(20개)가 아니라 **누적 내용량**으로 걸리는 것으로 보인다.

**대응**

1. 남은 PDF 는 **프로젝트 지식 베이스**에 올린다 (채팅 첨부와 별도 저장소, 개수 제한 없음).
2. 같은 프로젝트에서 **새 채팅**을 연다.
3. 새 채팅 첫 메시지에는 **zip 과 이 노트만** 첨부한다 (텍스트 추출량이 적어 부담이 작음).
4. PDF 대신 **텍스트/마크다운으로 변환**해서 올리면 훨씬 많이 들어간다.

---

## 8. 작업 방식 메모

- 방향과 주요 결정만 확인받고, 그 뒤 전체 구현은 자율로 진행한다.
- 정직성 우선: 실행하지 못한 것, H2 로 대체한 것, 랜덤 벡터로 흉내낸 것은
  반드시 `docs/02-정직한-고지.md` 에 남긴다. "검증한 척"을 만들지 않는다.
- 답변은 한국어로.
